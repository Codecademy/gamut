{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "styleguide",
  "sourceRoot": "apps/styleguide/stories",
  "projectType": "application",
  "targets": {
    "build-storybook": {
      "outputs": ["{options.outputPath}"],
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "cwd": "apps/styleguide",
        "outputPath": "./dist",
        "commands": [
          "NODE_ENV=production ./node_modules/.bin/build-storybook --quiet -c .storybook -o ./dist"
        ],
        "parallel": false
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/styleguide"],
      "options": {
        "jestConfig": "apps/styleguide/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
