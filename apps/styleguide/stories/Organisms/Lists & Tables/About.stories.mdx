import title from '@codecademy/macros/lib/title.macro';
import { Meta } from '@storybook/addon-docs/blocks';

import { TableOfContents } from '~styleguide/blocks';

<Meta
  title={title}
  parameters={{
    subtitle:
      'Lists & Tables are both organisms that render repeated, interactive information.',
  }}
/>

Both `List` and `Table` are commonly used to showcase objects, data, and information in powerful ways. While both Lists and Tables are similar and share the same underlying DataList structure, they both have different presentational templates and design best practices that will help you determine which component to use. Read on to figure out how to choose the best information design for your elements.

<TableOfContents />
