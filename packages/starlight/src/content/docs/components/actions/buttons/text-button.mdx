---
title: TextButton
description: A button that usually only has text and is used for tertiary actions.
---

import { Aside } from '@astrojs/starlight/components';

import FigmaEmbed from '~/components/FigmaEmbed.astro';
import StoryEmbed from '~/components/StoryEmbed.astro';

**Status:** Current · [Figma](https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=1106%3A0) · [Source](https://github.com/Codecademy/gamut/blob/main/packages/gamut/src/Button/TextButton.tsx)

<Aside type="tip">
  Use the `/gamut-buttons` skill in Cursor or Claude Code for AI-assisted
  guidance on button selection, variants, disabled patterns, and ToolTip focus
  management.
</Aside>

## Usage

Use this for tertiary actions. Include a leading or trailing icon from our mini icon set to clarify an action or to distinguish the button from other bold text.

**Best practices:**

- Our text buttons use bold formatting to differentiate themselves from normal text, as color alone does not meet the 3:1 contrast ratio required for accessibility. However, bold formatting may not be sufficient in all contexts, such as when placed alongside a bold heading.
- In these cases, we recommend including a leading or trailing icon to further distinguish the button from other bold text. This additional visual cue ensures that the text button is easily identifiable and accessible to all users.
- Hover, active, and disabled colors are handled by the component — don't override state colors with `color`/`bg` props.

**When NOT to use:**

- **Primary / high-emphasis actions** — use [FillButton](/components/actions/buttons/fill-button/) instead.
- **Secondary / outlined actions** — use [StrokeButton](/components/actions/buttons/stroke-button/) instead.
- **Icon-only actions** — use [IconButton](/components/actions/buttons/icon-button/) instead; it requires an accessible name via its `tip` prop.
- **Marketing / high-visibility CTAs** — use [CTAButton](/components/actions/buttons/cta-button/) instead.
- **Outside its color-mode context** — don't place light-mode buttons on a navy background; wrap the surface in `Background` or pass `mode` instead of styling around it.

### Anatomy

The full button spec — variants, sizes, and states — in the Gamut Figma file:

<FigmaEmbed url="https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=1106%3A0" />

## Patterns

### Rendering as a link

If `href` is defined, the component renders an anchor tag instead of a `<button>`:

```tsx
<TextButton href="/learn">View all courses</TextButton>
```

Note: `href` combined with `disabled` renders a `<button disabled>` — a link-style button cannot stay an anchor while disabled.

### Leading or trailing icon

Pass `icon` (from the mini icon set, for legibility at button sizes) and optionally `iconPosition` to align it to either side of the label. This is the recommended way to distinguish a text button from surrounding bold text (see Best practices):

```tsx
import { MiniArrowRightIcon } from '@codecademy/gamut-icons';

<TextButton icon={MiniArrowRightIcon} iconPosition="right">
  Next
</TextButton>;
```

### Disabled with a readable tooltip

The `disabled` prop removes the button from the tab order, so a tooltip explaining _why_ it's disabled can't be reached by keyboard. In that case use `aria-disabled` (without `disabled`) — Gamut's disabled styles also match `[aria-disabled='true']`:

```tsx
<ToolTip id="why-disabled" info="Complete the lesson first">
  <TextButton aria-describedby="why-disabled" aria-disabled>
    Skip
  </TextButton>
</ToolTip>
```

## Variants

<Aside type="caution" title="Content migration pending">
  Rendered examples for each variant and size live in the current Storybook
  stories. Until they're ported, see the [existing Button
  story](https://github.com/Codecademy/gamut/blob/main/packages/styleguide/src/lib/Atoms/Buttons/Button/Button.mdx)
  for `primary`/`secondary` canvases and the [Figma
  grid](https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=1106%3A0)
  for a view of all variants.
</Aside>

### variant

`primary` (default) · `secondary` · `danger` · `interface`

Unlike the other variants, `interface` renders in the default text color at rest, only taking on its variant color on hover and focus.

### size

`normal` (default) · `small` · `large`

### mode

Supports both `light` and `dark` color modes; `mode` forces one regardless of the surrounding context.

## Accessibility

- Renders a native `<button>` (or `<a>` when `href` is set), so keyboard reachability and activation come for free.
- The visible label (`children`) is the accessible name. For icon-only actions, use [IconButton](/components/actions/buttons/icon-button/) instead, which requires a name via `tip`.
- The bold label is what distinguishes the button from surrounding text, since color alone doesn't meet the 3:1 non-text contrast requirement — add an `icon` when the surrounding context is also bold (see Best practices).
- Prefer `aria-disabled` over `disabled` when a disabled button must stay focusable — for example, to keep an explanatory ToolTip readable (see Patterns).

## Props

| Prop           | Type                                                  | Default     | Description                                                           |
| -------------- | ----------------------------------------------------- | ----------- | --------------------------------------------------------------------- |
| `children`     | `React.ReactNode`                                     | —           | The button's label.                                                   |
| `variant`      | `'primary' \| 'secondary' \| 'danger' \| 'interface'` | `'primary'` | Color emphasis.                                                       |
| `size`         | `'normal' \| 'small' \| 'large'`                      | `'normal'`  | Height, padding, and font size.                                       |
| `mode`         | `ColorModes` (`'light' \| 'dark'`)                    | inherited   | Forces which color mode's palette the button uses.                    |
| `icon`         | Gamut icon component                                  | —           | A single inline icon, rendered alongside the label. Use the mini set. |
| `iconPosition` | `'left' \| 'right'`                                   | `'left'`    | Which side of the label the icon renders on.                          |
| `href`         | `string`                                              | —           | If defined, component will use an anchor tag.                         |
| `disabled`     | `boolean`                                             | `false`     | Disables the button (see Accessibility for the `aria-disabled` case). |
| `onClick`      | `HTMLProps<HTMLButtonElement>['onClick']`             | —           | Click handler.                                                        |

`TextButton` also accepts Gamut's `layout`, `positioning`, `space`, and `border` system props.

## Playground

The example below is embedded from the deployed Storybook. For the full
playground with prop controls, open the
[TextButton story](https://gamut.codecademy.com/?path=/docs/atoms-buttons-textbutton--docs)
directly.

<StoryEmbed id="atoms-buttons-textbutton--default" />
