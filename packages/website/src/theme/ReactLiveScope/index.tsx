import { Alert, FillButton, StrokeButton, TextButton } from '@codecademy/gamut';
import { MiniArrowRightIcon } from '@codecademy/gamut-icons';
import React from 'react';

/**
 * Components available inside ```jsx live code blocks across the docs.
 * Add a component here once its page needs a live example.
 */
const ReactLiveScope = {
  ...React,
  Alert,
  FillButton,
  StrokeButton,
  TextButton,
  MiniArrowRightIcon,
};

export default ReactLiveScope;
