---
title: Typography
description: The Text component that renders the type scale.
---

import { Callout } from 'nextra/components';

# Typography

`Text` — renders the type scale as an element, with props for size,
weight, and semantic tag. See
[Reference → Design tokens](/reference/design-tokens) for the
underlying scale.

<Callout type="info">
  Stub — individual component page hasn't been migrated yet. Tracked under ADR
  0001's migration.
</Callout>
