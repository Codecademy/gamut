{
  "name": "@codecademy/gamut-icons",
  "description": "Icon library for codecademy.com",
  "version": "2.0.1",
  "author": "Codecademy <dev@codecademy.com>",
  "main": "icons/index.js",
  "module": "icons/index.js",
  "sideEffects": false,
  "engines": {
    "node": ">=6.0.0",
    "npm": ">=3.5.2"
  },
  "scripts": {
    "format": "svgo -f ./src --pretty --config=.svgo.yml",
    "clean": "rm -rf ./icons; rm -rf ./tmp; mkdir ./tmp",
    "build:icons": "svgr --out-dir ./tmp/ts --ext 'tsx' ./src",
    "build:types": "tsc --emitDeclarationOnly",
    "build:compile": "babel ./tmp/ts --out-dir ./icons --copy-files --extensions \".ts,.tsx\"",
    "rename-icons": "find ./src -depth -name '*.svg -not *-icon.svg' -exec sh -c 'mv \"$1\" \"${1%.svg}-icon.svg\"' _ {} \\;",
    "build": "yarn clean && yarn build:icons && yarn rename-icons && yarn build:compile && yarn build:types"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:RyzacInc/client-modules.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@svgr/cli": "5.0.0",
    "create-index": "^2.5.0",
    "replace-in-file": "^5.0.2",
    "svgo": "^1.3.2",
    "typescript": "*"
  }
}
