import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { Text } from '@codecademy/gamut/src';
import { brandColors } from '@codecademy/gamut-styles';
import title from '@codecademy/macros/lib/title.macro';

<Meta
  title={title}
  component={Text}
  parameters={{
    subtitle: `This primitive can be configured as any possible combination of our typography tokens`,
    status: 'deprecated',
    source: 'gamut',
  }}
  argTypes={{
    fontSize: {
      label: 'Font Size',
      control: { type: 'select', options: ['sm', 'md', 'lg'] },
    },
  }}
/>

<Canvas>
  <Story name="Text" args={{ children: 'Hello World' }}>
    {(args) => <Text {...args} />}
  </Story>
</Canvas>

## Props

<Props story="Text" />

## Custom colors

<Canvas>
  <Story name="Custom Colors">
    {(args) => (
      <Text fontSize="md" style={{ color: brandColors.purple }}>
        Brand Purple
      </Text>
    )}
  </Story>
</Canvas>
