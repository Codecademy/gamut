import { TextDeprecated } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta
  title={title}
  component={TextDeprecated}
  parameters={{
    subtitle: `This primitive can be configured as any possible combination of our typography tokens`,
    status: 'deprecated',
    source: 'gamut',
  }}
  argTypes={{
    fontSize: {
      label: 'Font Size',
      control: { type: 'select', options: ['sm', 'md', 'lg'] },
    },
  }}
/>

<Canvas>
  <Story name="TextDeprecated" args={{ children: 'Hello World' }}>
    {(args) => <TextDeprecated {...args} />}
  </Story>
</Canvas>

## Props

<PropsTable story="TextDeprecated" />
