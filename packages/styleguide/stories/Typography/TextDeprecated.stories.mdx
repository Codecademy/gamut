import { DeprecatedText } from '@codecademy/gamut/src';
import { brandColors } from '@codecademy/gamut-styles';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/dist/blocks';

<Meta
  title={title}
  component={DeprecatedText}
  parameters={{
    subtitle: `This primitive can be configured as any possible combination of our typography tokens`,
    status: 'deprecated',
    source: 'gamut',
  }}
  argTypes={{
    fontSize: {
      label: 'Font Size',
      control: { type: 'select', options: ['sm', 'md', 'lg'] },
    },
  }}
/>

<Canvas>
  <Story name="DeprecatedText" args={{ children: 'Hello World' }}>
    {(args) => <DeprecatedText {...args} />}
  </Story>
</Canvas>

## Props

<Props story="DeprecatedText" />

## Custom colors

<Canvas>
  <Story name="Custom Colors">
    {(args) => (
      <DeprecatedText fontSize="md" style={{ color: brandColors.purple }}>
        Brand Purple
      </DeprecatedText>
    )}
  </Story>
</Canvas>
