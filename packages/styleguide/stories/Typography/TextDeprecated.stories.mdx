import { TextDeprecated } from '@codecademy/gamut/src';
import { brandColors } from '@codecademy/gamut-styles';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={TextDeprecated}
  parameters={{
    subtitle: `This primitive can be configured as any possible combination of our typography tokens`,
    status: 'deprecated',
    source: 'gamut',
  }}
  argTypes={{
    fontSize: {
      label: 'Font Size',
      control: { type: 'select', options: ['sm', 'md', 'lg'] },
    },
  }}
/>

<Canvas>
  <Story name="TextDeprecated" args={{ children: 'Hello World' }}>
    {(args) => <TextDeprecated {...args} />}
  </Story>
</Canvas>

## Props

<Props story="TextDeprecated" />

## Custom colors

<Canvas>
  <Story name="Custom Colors">
    {(args) => (
      <TextDeprecated fontSize="md" style={{ color: brandColors.purple }}>
        Brand Purple
      </TextDeprecated>
    )}
  </Story>
</Canvas>
