import { HeadingDeprecated, Text } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Meta, Preview, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={HeadingDeprecated}
  parameters={{
    subtitle: `This is a primitive to handle all \`h1-h6\` tags
with defined size intervals to allow for semantic html that also
matches the designs.`,
    status: 'deprecated',
    source: 'gamut',
  }}
  argTypes={{
    as: {
      label: 'As element',
      control: {
        type: 'select',
        options: ['h1', 'h2', 'h3', 'h4', 'h5', 'h6'],
      },
    },
    fontSize: {
      label: 'Font Size',
      control: {
        type: 'select',
        options: ['xs', 'sm', 'md', 'lg', 'xl', 'xxl'],
      },
    },
  }}
/>

Note: Please only [increase heading levels by 1](https://dequeuniversity.com/rules/axe/3.0/heading-order)
(and pick the appropriate size to match the design).

<Preview>
  <Story
    name="HeadingDeprecated"
    args={{ children: 'Hello World', as: 'h1', hideMargin: true }}
  >
    {(args) => <HeadingDeprecated {...args} />}
  </Story>
</Preview>

## Props

<PropsTable story="HeadingDeprecated" />

## No Margin

<Preview>
  <Story name="No Margin">
    {() => (
      <>
        <HeadingDeprecated fontSize="md" as="h3" hideMargin>
          Without Margin
        </HeadingDeprecated>
        <Text>Random other text</Text>
      </>
    )}
  </Story>
</Preview>
