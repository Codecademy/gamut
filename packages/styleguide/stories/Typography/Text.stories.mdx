import { Box, GridBox, Text } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import { Fragment } from 'react';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={Text}
  parameters={{
    status: 'updating',
    subtitle: `This is a single primitive to handle all \`<p>\` and
\`<span>\` tags with defined size intervals. Renders a \`<span>\` by default.`,
    source: 'gamut',
  }}
/>

<Canvas>
  <Story name="Text" args={{ children: 'Hello World' }}>
    {(args) => <Text {...args} />}
  </Story>
</Canvas>

## Props

<PropsTable story="Text" defaultGroups={['typography']} />

## Variants

export const tags = [
  'h1',
  'h2',
  'h3',
  'h4',
  'h5',
  'h6',
  'small',
  'p',
  'strong',
  'code',
  'span',
];

<Canvas>
  <Story name="Variants">
    {(args) => (
      <GridBox
        padding={8}
        rowGap={24}
        columnGap={32}
        gridTemplateColumns="max-content 1fr"
        alignItems="center"
      >
        {tags.map((tag) => (
          <Fragment key={tag}>
            <Box>
              <Text as="code" fontSize={18}>
                {tag}
              </Text>
            </Box>
            <Box>
              <Text as={tag}>Lorem Ipsum Dolor</Text>
            </Box>
          </Fragment>
        ))}
      </GridBox>
    )}
  </Story>
</Canvas>
