import { NewText } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={NewText}
  parameters={{
    subtitle: `This is a single primitive to handle all \`<p>\` and
\`<span>\` tags with defined size intervals. Renders a \`<span>\` by default.`,
    source: 'gamut-labs',
  }}
/>

<Canvas>
  <Story name="NewText" args={{ children: 'Hello World' }}>
    {(args) => <NewText {...args} />}
  </Story>
</Canvas>

## Props

<PropsTable story="Text" defaultGroups={['typography']} />
