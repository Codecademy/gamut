import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { GlobalHeader } from '@codecademy/gamut-labs/src/experimental/GlobalHeader/index.tsx';
import { IconButton } from '@codecademy/gamut/src';
import { BellIcon, SearchIcon, PersonIcon } from '@codecademy/gamut-icons/src';
import { colors } from '@codecademy/gamut-styles';

<Meta
  title="Labs/Experimental/Molecules/GlobalHeader"
  component={GlobalHeader}
  parameters={{ layout: 'padded', status: 'volatile' }}
/>

Header for a unified Codecademy global navigation system.

## GlobalHeader

<Canvas withSource="open">
  <Story name="GlobalHeader">
    {(args) => <GlobalHeader variant={{ type: 'free' }} />}
  </Story>
</Canvas>

<Props of={GlobalHeader} />

## Free User

<Canvas>
  <Story name="Free User Global Header">
    {(args) => <GlobalHeader variant={{ type: 'free' }} />}
  </Story>
</Canvas>

## Pro User

<Canvas>
  <Story name="Pro User Global Header">
    {(args) => <GlobalHeader variant={{ type: 'pro' }} />}
  </Story>
</Canvas>

## Anonymous User

<Canvas>
  <Story name="Anonymous User Global Header">
    {(args) => <GlobalHeader variant={{ type: 'anon' }} />}
  </Story>
</Canvas>

## GlobalHeader With Search, Notifications, and Profile Dropdown

To mock this, we're providing IconButtons as placeholders.

<Canvas>
  <Story name="GlobalHeader Search Notifications Profile">
    {(args) => (
      <GlobalHeader
        variant={{
          type: 'pro',
          renderSearch: () => <IconButton icon={SearchIcon} />,
          renderNotifications: () => <IconButton icon={BellIcon} />,
          renderProfile: () => <IconButton icon={PersonIcon} />,
        }}
      />
    )}
  </Story>
</Canvas>

## GlobalHeader Over Background Color

<Canvas>
  <Story name="Background Color GlobalHeader">
    {(args) => (
      <div style={{ backgroundColor: colors.beige, width: '100%' }}>
        <GlobalHeader variant={{ type: 'anon' }} />
      </div>
    )}
  </Story>
</Canvas>

## GlobalHeader Can Accept an onClick Function

To mock this, we're providing a very basic onClick function which just prints out the event & the tracking target to the console.

<Canvas>
  <Story name="Passing an OnClick to GlobalHeader">
    {(args) => (
      <GlobalHeader
        variant={{ type: 'anon' }}
        onClick={(event, trackingTarget) => {
          console.log(event);
          console.log(trackingTarget);
        }}
      />
    )}
  </Story>
</Canvas>
