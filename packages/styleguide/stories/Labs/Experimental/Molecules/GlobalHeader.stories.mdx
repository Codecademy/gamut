import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { StoryStatus } from '~styleguide/blocks';
import { GlobalHeader } from '@codecademy/gamut-labs/src/experimental/GlobalHeader/index.tsx';

<Meta
  title="Labs/Experimental/Molecules/GlobalHeader"
  component={GlobalHeader}
/>

# GlobalHeader

<StoryStatus status="volatile" />

Header for a unified Codecademy global navigation system.

## GlobalHeader

<Canvas withSource="open">
  <Story name="GlobalHeader">
    {(args) => (
      <GlobalHeader
        user={{
          avatar: '',
          roles: {
            isAdmin: false,
            isCustomerService: false,
            isPro: false,
          },
          displayName: 'Codey',
        }}
      />
    )}
  </Story>
</Canvas>

<Props of={GlobalHeader} />

## Free User

<Canvas>
  <Story name="Free User Global Header">
    {(args) => (
      <GlobalHeader
        user={{
          avatar: '',
          roles: {
            isAdmin: false,
            isCustomerService: false,
            isPro: false,
          },
          displayName: 'Free Codey',
        }}
      />
    )}
  </Story>
</Canvas>

## Pro User

<Canvas>
  <Story name="Pro User Global Header">
    {(args) => (
      <GlobalHeader
        user={{
          avatar: '',
          roles: {
            isAdmin: true,
            isCustomerService: true,
            isPro: true,
          },
          displayName: 'Pro Codey',
        }}
      />
    )}
  </Story>
</Canvas>

## Anonymous User

<Canvas>
  <Story name="Anonymous User Global Header">
    {(args) => <GlobalHeader />}
  </Story>
</Canvas>
