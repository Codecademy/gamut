import { AppHeaderLink } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';
import LinkTo from '@storybook/addon-links/dist/react/components/link';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={AppHeaderLink}
  parameters={{
    layout: 'padded',
    status: 'volatile',
    source: 'gamut-labs',
    subtitle:
      'A styled Anchor for use in the top-level AppHeader (not in dropdowns or the mobile menu).',
  }}
  args={{
    item: {
      id: 'link',
      href: 'http://codecademy.com',
      text: 'Nav Link',
      trackingTarget: 'tracking-target',
      type: 'link',
    },
  }}
/>

A simple link for use in the top-level of <LinkTo kind="Labs/Experimental/Molecules/AppHeader" story="AppHeader">AppHeader</LinkTo> (i.e., not in dropdowns or the mobile menu).

<Canvas>
  <Story name="AppHeaderLink">{(args) => <AppHeaderLink {...args} />}</Story>
</Canvas>

<PropsTable story="AppHeaderLink" />

## Link in Current Tab

<Canvas>
  <Story
    name="Current Tab"
    args={{
      item: {
        id: 'link-current-tab',
        href: 'http://codecademy.com',
        text: 'Current Tab',
        trackingTarget: 'tracking-target',
        type: 'link',
      },
    }}
  >
    {(args) => <AppHeaderLink {...args} />}
  </Story>
</Canvas>

## Link in New Tab

<Canvas>
  <Story
    name="New Tab"
    args={{
      item: {
        id: 'link-new-tab',
        href: 'http://codecademy.com',
        newTab: true,
        text: 'New Tab',
        trackingTarget: 'tracking-target',
        type: 'link',
      },
    }}
  >
    {(args) => <AppHeaderLink {...args} />}
  </Story>
</Canvas>
