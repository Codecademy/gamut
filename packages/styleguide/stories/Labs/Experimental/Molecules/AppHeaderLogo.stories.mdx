import { AppHeaderLogo } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={AppHeaderLogo}
  parameters={{
    layout: 'padded',
    status: 'volatile',
    source: 'gamut-labs',
    subtitle: 'A Codecademy logo button for use in AppHeader.',
  }}
  args={{
    item: {
      pro: true,
      href: 'http://codecademy.com',
      trackingTarget: 'test',
      type: 'logo',
    },
  }}
/>

<Canvas>
  <Story name="AppHeaderLogo">{(args) => <AppHeaderLogo {...args} />}</Story>
</Canvas>

<PropsTable story="AppHeaderLogo" />

## Pro

<Canvas>
  <Story name="Pro">{(args) => <AppHeaderLogo {...args} />}</Story>
</Canvas>

## Free + Anon

<Canvas>
  <Story
    name="Free + Anon"
    args={{
      item: {
        pro: false,
        href: 'http://codecademy.com',
        trackingTarget: 'test',
        type: 'logo',
      },
    }}
  >
    {(args) => <AppHeaderLogo {...args} />}
  </Story>
</Canvas>
