import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { text, boolean } from '@storybook/addon-knobs';
import { StoryStatus } from '~styleguide/blocks';
import { PageSectionTitle } from '@codecademy/gamut-labs/src';

<Meta
  title="Labs/LandingPage/Atoms/PageSectionTitle"
  component={PageSectionTitle}
/>

# PageSectionTitle

<StoryStatus status="unstable" />

This is title field used in sections on a marketing page.

<Canvas withSource="open">
  <Story name="PageSectionTitle">
    {(args) => (
      <PageSectionTitle isPageHeading={boolean('isPageHeading', false)}>
        {text('Text', 'Lorem ipsum dolor sit amet')}
      </PageSectionTitle>
    )}
  </Story>
</Canvas>

<Props of={PageSectionTitle} />
