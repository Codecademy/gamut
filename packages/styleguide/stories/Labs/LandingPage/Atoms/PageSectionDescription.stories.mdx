import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { text } from '@storybook/addon-knobs';
import { StoryStatus } from '~styleguide/blocks';
import { PageSectionDescription } from '@codecademy/gamut-labs/src';

<Meta
  title="Labs/LandingPage/Atoms/PageSectionDescription"
  component={PageSectionDescription}
  parameters={{ knobs: { escapeHTML: false } }}
/>

# PageSectionDescription

<StoryStatus status="unstable" />

This is a description field used in sections on a marketing page.

<Canvas withSource="open">
  <Story name="PageSectionDescription">
    {(args) => (
      <PageSectionDescription
        text={text(
          'markdown',
          `
## Hello World
This is markdown
          `
        )}
      />
    )}
  </Story>
</Canvas>

<Props of={PageSectionDescription} />
