import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { text } from '@storybook/addon-knobs';
import { StoryStatus } from '~styleguide/blocks';
import { PageSectionCTA } from '@codecademy/gamut-labs/src';

<Meta
  title="Labs/LandingPage/Atoms/PageSectionCTA"
  component={PageSectionCTA}
/>

# PageSectionCTA

<StoryStatus status="unstable" />

This is a cta used in sections on a marketing page.

<Canvas withSource="open">
  <Story name="PageSectionCTA">
    {(args) => (
      <PageSectionCTA href={text('href', 'https://example.com/')}>
        {text('Text', 'Lorem ipsum dolor sit amet')}
      </PageSectionCTA>
    )}
  </Story>
</Canvas>

<Props of={PageSectionCTA} />
