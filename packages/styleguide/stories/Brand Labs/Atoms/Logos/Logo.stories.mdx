import { Logo } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={Logo}
  parameters={{
    subtitle: `Our proud logo. There is no a in the middle. Use sparingly, and make sure
it has a lot of visual distinction to stand out appropriately.`,
    source: 'gamut-labs',
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=465%3A163',
    },
  }}
/>

<Canvas withSource="open">
  <Story name="Logo">{(args) => <Logo {...args} />}</Story>
</Canvas>

<PropsTable story="Logo" />

Variants of the Codecademy logo you can play around with.

<Canvas>
  <Story name="Default" args={{ variant: 'default' }}>
    {(args) => <Logo {...args} />}
  </Story>
  <Story name="Pro" args={{ variant: 'pro' }}>
    {(args) => <Logo {...args} />}
  </Story>
  <Story name="Mini" args={{ variant: 'mini' }}>
    {(args) => <Logo {...args} />}
  </Story>
  <Story name="Enterprise" args={{ variant: 'enterprise', height: '36px' }}>
    {(args) => <Logo {...args} />}
  </Story>
</Canvas>
