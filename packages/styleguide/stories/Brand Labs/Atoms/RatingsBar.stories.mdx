import { RatingsBar } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={RatingsBar}
  parameters={{
    subtitle: 'A horizontal bar for displaying percentage of ratings',
    source: 'gamut-labs',
  }}
  args={{ minimumPercent: 5, percent: 75 }}
/>

RatingsBars are to be used for pages where ratings have been submitted for a course.

<Canvas>
  <Story name="RatingsBar">{(args) => <RatingsBar {...args} />}</Story>
</Canvas>

<Props story="RatingsBar" />
