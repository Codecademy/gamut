import { RatingsDistribution } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={RatingsDistribution}
  parameters={{
    subtitle: 'Generates a ratings distribution summary.',
    source: 'gamut-labs',
  }}
  args={{
    totalCount: 4363,
    counts: {
      fives: 911,
      fours: 1625,
      threes: 922,
      twos: 22,
      ones: 883,
    },
  }}
/>

<Canvas>
  <Story name="RatingsDistribution">
    {(args) => <RatingsDistribution {...args} />}
  </Story>
</Canvas>

<Props story="RatingsDistribution" />
