import { Banner } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={Banner}
  parameters={{
    subtitle: `Full-width banner to indicate important messaging at the top of a page or section.`,
    status: 'current',
    source: 'gamut-labs',
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/wTwIV8YfAgZ8eNpKSEd0Gn',
    },
  }}
  args={{
    children:
      'Happy Holidays! We have a Student discount for Codecademy **Pro**. [Learn More](https://gamut.codecademy.com)',
  }}
/>

<Canvas>
  <Story name="Banner">{(args) => <Banner {...args} />}</Story>
</Canvas>

<PropsTable story="Banner" />

## Variants

<Canvas isColumn>
  <Story name="Navy" args={{ children: 'Navy', variant: 'navy' }}>
    {(args) => <Banner {...args} />}
  </Story>
  <Story name="Yellow" args={{ children: 'Yellow', variant: 'yellow' }}>
    {(args) => <Banner {...args} />}
  </Story>
</Canvas>

## Call to Action

<Canvas isColumn>
  <Story name="Call to Action" args={{ cta: 'Learn more' }}>
    {(args) => <Banner {...args} />}
  </Story>
</Canvas>
