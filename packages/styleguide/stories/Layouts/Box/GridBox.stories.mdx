import { Box, GridBox } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';
import LinkTo from '@storybook/addon-links/dist/react/components/link';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={GridBox}
  parameters={{ status: 'volatile', source: 'gamut' }}
/>

<p>
  For Grid layouts we&apos;ve added a specific flex <code>GridBox</code>{' '}
  component that extends{' '}
  <code>
    <LinkTo kind="Layouts/Box">Box</LinkTo>
  </code>{' '}
  with added flex props.
</p>

<Canvas>
  <Story
    name="GridBox"
    args={{
      rowGap: 16,
    }}
  >
    {({ as = 'div', ...args }) => {
      const elProps = {
        borderWidth: '1px',
        borderStyle: 'solid',
        borderColor: 'navy',
        padding: 16,
      };
      return (
        <GridBox {...args}>
          <Box {...elProps}>Child Element 1</Box>
          <Box {...elProps}>Child Element 2</Box>
        </GridBox>
      );
    }}
  </Story>
</Canvas>

<PropsTable story="GridBox" defaultGroups={['grid']} />

## Example

<Canvas>
  <Story name="4 Column Grid">
    {(args) => {
      const elProps = {
        borderWidth: '1px',
        borderStyle: 'solid',
        borderColor: 'navy',
        padding: 16,
      };
      return (
        <GridBox gridAutoFlow="column" gridAutoColumns="1fr" columnGap={16}>
          <Box {...elProps}>Child Element 1</Box>
          <Box {...elProps}>Child Element 2</Box>
          <Box {...elProps}>Child Element 3</Box>
          <Box {...elProps}>Child Element 4</Box>
        </GridBox>
      );
    }}
  </Story>
</Canvas>
