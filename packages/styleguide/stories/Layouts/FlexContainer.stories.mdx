import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { Item, Container } from '@codecademy/gamut/src';
import { ExampleBox, defaultGridProps } from './Elements';
import title from '../../.storybook/title.macro.js';

<Meta
  title={title}
  component={Container}
  subcomponents={{ Container, Item }}
  parameters={{
    source: 'gamut',
    subtitle: 'Flexbox Container component with all system props available',
  }}
/>

A flex box primitive that allows you create markup configurable flex containers

<Canvas>
  <Story name="Flex Container">
    {(args) => {
      return (
        <Container {...defaultGridProps} {...args}>
          <ExampleBox>Element 1</ExampleBox>
          <ExampleBox>Element 2</ExampleBox>
        </Container>
      );
    }}
  </Story>
</Canvas>

<Props story="Flex Container" />
