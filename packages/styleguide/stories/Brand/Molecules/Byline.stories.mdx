import { Byline } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={Byline}
  args={{
    firstName: 'Bill',
    lastName: 'Murray',
    occupation: 'Ghostbuster @ Ghostcademy',
    location: 'New York, NY',
  }}
  parameters={{
    subtitle: `Marketing display of a person's information, such as for a testimonial.
Commonly paired with an \`Avatar\`.`,
    source: 'gamut-labs',
  }}
/>
;

<Canvas withSource="open">
  <Story name="Byline">{(args) => <Byline {...args} />}</Story>
</Canvas>

<PropsTable story="Byline" />
