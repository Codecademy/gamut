import { Heading } from '@codecademy/gamut/src';
import { BrandMonospace } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/dist/blocks';

import styles from './styles.module.scss';

<Meta
  title={title}
  component={BrandMonospace}
  parameters={{
    subtitle: `A wrapper component that will style text with the \`Robot Mono\`
font.`,
    status: 'volatile',
    source: 'gamut-labs',
  }}
/>

Currently, Codecademy uses `Monaco` as our default monospaced
font. However, there are a few brand related instances where we are experimenting
with using `Roboto Mono`.

<Canvas>
  <Story
    name="BrandMonospace"
    args={{ children: 'The quick brown fox jumps over the lazy dog' }}
  >
    {(args) => (
      <>
        <BrandMonospace>
          <Heading fontSize="xl" as="h1">
            Roboto Mono
          </Heading>
          <Heading fontSize="lg" as="h2">
            {args.children}
          </Heading>
        </BrandMonospace>
        <Heading fontSize="xl" as="h1" className={styles.monacoFont}>
          Monaco
        </Heading>
        <Heading fontSize="lg" as="h2" className={styles.monacoFont}>
          {args.children}
        </Heading>
      </>
    )}
  </Story>
</Canvas>
