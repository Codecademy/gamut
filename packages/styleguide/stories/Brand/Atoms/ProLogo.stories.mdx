import { ProLogo } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { PropsTable, selectableColorNames } from '~styleguide/blocks';

<Meta
  title={title}
  component={ProLogo}
  argTypes={{
    backgroundColor: {
      control: { type: 'select', options: selectableColorNames },
    },
    cutoutColor: {
      control: { type: 'select', options: selectableColorNames },
    },
  }}
  args={{
    variant: 'cutout',
  }}
  parameters={{
    subtitle: `This Pro logo will be used in the most recent designs for Curriculum Cards and Course overview pages.`,
    source: 'gamut-labs',
    status: 'updating',
  }}
/>

<Canvas withSource="open">
  <Story name="ProLogo">{(args) => <ProLogo {...args} />}</Story>
</Canvas>

<PropsTable story="ProLogo" />
