import { PageVideoGallery } from '@codecademy/gamut-labs/src';
import { Background } from '@codecademy/gamut-styles/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={PageVideoGallery}
  args={{
    videos: [
      {
        title: 'Steppin on the M83',
        url: 'https://www.youtube.com/watch?v=-m2fVWj99x4',
        placeholderImage:
          'https://i.kym-cdn.com/entries/icons/mobile/000/010/275/x1080.jpg',
      },
      {
        title: 'Once in a Lifetime performed by Kermit the Frog',
        url: 'https://www.youtube.com/watch?v=PCY0aeUx-Ns',
        placeholderImage:
          'https://i.kinja-img.com/gawker-media/image/upload/c_fill,f_auto,fl_progressive,g_center,h_675,pg_1,q_80,w_1200/slvzej9jmwy1ze8nknmr.jpg',
      },
      {
        title: 'Spooky Scary Skeletons',
        url: 'https://www.youtube.com/watch?v=K2rwxs1gH9w',
        placeholderImage:
          'https://image.pbs.org/video-assets/UWTcFJA-asset-mezzanine-16x9-NfmXh7O.jpg',
      },
    ],
    title: 'You may find yourself living in a shotgun shack',
    desc:
      'And you may find yourself in another part of the world, and you may find yourself behind the wheel of a large automobile',
    cta: {
      text: 'Same as it ever was',
      href: 'https://codecademy.com',
    },
    mode: 'light',
  }}
  parameters={{
    subtitle: `Video Gallery component for landing pages created in Contentful. Videos are lazy-loaded with optionally configurable placeholder images.`,
    status: 'updating',
  }}
/>

<Canvas>
  <Story name="Video Gallery">{(args) => <PageVideoGallery {...args} />}</Story>
</Canvas>

<PropsTable story="Video Gallery" />

## Regular Button

Pass `buttonType` as `fill` to show a `FillButton` instead of the larger `CTAButton`. Text should always be Call to Action text regardless of button type.

<Canvas>
  <Story name="Regular Button">
    {(args) => (
      <PageVideoGallery {...args} cta={{ ...args.cta, buttonType: 'fill' }} />
    )}
  </Story>
</Canvas>

## Without Description

Omit the `desc` prop to not show a description.

<Canvas>
  <Story name="Without Description">
    {(args) => <PageVideoGallery {...args} desc={undefined} />}
  </Story>
</Canvas>

## Gallery only

<Canvas>
  <Story name="Gallery Only">
    {(args) => <PageVideoGallery videos={args.videos} />}
  </Story>
</Canvas>

## Dark Mode

Automatically adjusts to the current Color Mode. No configuration is needed to meet contrast and design guidelines.

<Canvas>
  <Story name="Dark Mode">
    {(args) => (
      <Background bg="navy" p={16}>
        <PageVideoGallery {...args} />
      </Background>
    )}
  </Story>
</Canvas>
