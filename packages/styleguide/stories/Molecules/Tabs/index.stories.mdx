import { Text } from '@codecademy/gamut/src';
import { TabPanel, Tabs } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={Tabs}
  subcomponents={{ TabPanel }}
  parameters={{
    subtitle: `Use a series of tabbed areas for when you have multiple potential sections
to show, but only one should be visible at a time.`,
    source: 'gamut-labs',
    figmaId: 'fmirRfslSgkAyYz0WYBaF9',
    status: 'current',
  }}
  args={{
    activeTabIndex: 0,
  }}
/>

<Canvas>
  <Story name="Tabs">
    {(args) => (
      <Tabs {...args}>
        <TabPanel>
          <Text>Tab One</Text>
        </TabPanel>
        <TabPanel>
          <Text>Tab Two</Text>
        </TabPanel>
        <TabPanel>
          <Text>Tab Three</Text>
        </TabPanel>
      </Tabs>
    )}
  </Story>
</Canvas>

<PropsTable story="Tabs" />
