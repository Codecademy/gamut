import { Menu, MenuItem } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={MenuItem}
  parameters={{
    subtitle: 'A semantic menu item to be used with Menu',
    source: 'gamut',
    status: 'updating',
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/r5Y0iuLDFPXy8SmYX1UW2H',
    },
  }}
  args={{ children: 'Item' }}
  decorators={[(Story) => <Menu variant="select">{Story()}</Menu>]}
/>

## Code playground

Edit attributes and see what’s possible to do with this component or get the exact output you want.

<Canvas>
  <Story name="MenuItem">{(args) => <MenuItem {...args} />}</Story>
</Canvas>

<PropsTable story="MenuItem" />
