import { Menu, MenuSeparator } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={MenuSeparator}
  parameters={{
    subtitle: 'A semantic separator to be used with Menu',
    source: 'gamut',
    status: 'updating',
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/r5Y0iuLDFPXy8SmYX1UW2H',
    },
  }}
  decorators={[(Story) => <Menu variant="action">{Story()}</Menu>]}
  args={{}}
/>

## Code Playground

Edit attributes and see what’s possible to do with this component or get the exact output you want.

<Canvas>
  <Story name="MenuSeparator">{(args) => <MenuSeparator {...args} />}</Story>
</Canvas>

<PropsTable story="MenuSeparator" />
