import { DropdownButton } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

export const items = [
  { text: 'Item 1' },
  { text: 'Item 2' },
  { text: 'Item 3' },
  { text: 'Item 4' },
];

<Meta
  title={title}
  component={DropdownButton}
  parameters={{
    subtitle: 'A button',
    source: 'gamut-labs',
    status: 'updating',
  }}
  args={{
    buttonType: 'fill',
    dropdownItems: items,
  }}
/>

<Canvas>
  <Story name="DropdownButton">
    {(args) => <DropdownButton {...args}>click me</DropdownButton>}
  </Story>
</Canvas>

<PropsTable story="DropdownButton" />
