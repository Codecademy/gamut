import { StarIcon } from '@codecademy/gamut-icons';
import { DropdownButton } from '@codecademy/gamut-labs';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

export const items = [
  {
    text: 'List Item 1',
    icon: StarIcon,
    clickHandler: () => console.log('item 1'),
  },
  { text: 'List Item 2', icon: StarIcon },
  { text: 'List Item 3', icon: StarIcon },
  { text: 'List Item 4', icon: StarIcon },
];

<Meta
  title={title}
  component={DropdownButton}
  parameters={{
    subtitle: 'A button',
    source: 'gamut-labs',
    status: 'updating',
  }}
  args={{
    buttonType: 'fill',
    dropdownItems: items,
  }}
/>

<Canvas>
  <Story name="DropdownButton">
    {(args) => <DropdownButton {...args}>click me</DropdownButton>}
  </Story>
</Canvas>

<PropsTable story="DropdownButton" />
