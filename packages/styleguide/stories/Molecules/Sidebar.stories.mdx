import { Sidebar, AccordionButton } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/dist/blocks';

<Meta
  title={title}
  component={Sidebar}
  subcomponents={{ AccordionButton }}
  parameters={{
    subtitle: 'A sidebar for fun',
    status: 'volatile',
    source: 'gamut',
  }}
/>

<Canvas>
  <Story name="Sidebar">
    {(args) => (
      <Sidebar
        expanded={true}
        button={<AccordionButton expanded={true}>click</AccordionButton>}
        {...args}
      >
        Hidden treasure!
      </Sidebar>
    )}
  </Story>
</Canvas>

<Props story="Sidebar" />
