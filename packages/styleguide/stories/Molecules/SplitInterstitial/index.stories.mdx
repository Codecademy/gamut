import {
  DeprecatedHeading,
  SplitInterstitial,
  Text,
} from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/dist/blocks';

import Stairs from './assets/Stairs.svg';
import Triangle from './assets/Triangle.svg';

<Meta
  title={title}
  component={SplitInterstitial}
  parameters={{
    subtitle: `
Large, 50% split content area used for information that has two facets,
such as for a payment page with an advertisement on one side and payment
form on another.`,
    status: 'deprecated',
    source: 'gamut',
  }}
/>

<Canvas>
  <Story name="SplitInterstitial">
    {(args) => (
      <SplitInterstitial
        left={{
          children: (
            <>
              <DeprecatedHeading as="h1">Important stuff!</DeprecatedHeading>
              <Text>Some sort of information here, perhaps?</Text>
            </>
          ),
        }}
        right={{
          children: (
            <>
              <DeprecatedHeading as="h2">Less important...</DeprecatedHeading>
              <Text>Some more information here, maybe?</Text>
            </>
          ),
        }}
      />
    )}
  </Story>
</Canvas>

<Props story="SplitInterstitial" />

## Custom Images

You can override the built-in images with your own on a per-design basis.

<Canvas>
  <Story name="Custom Images">
    {(args) => (
      <SplitInterstitial
        left={{
          children: (
            <>
              <DeprecatedHeading as="h1">Important stuff!</DeprecatedHeading>
              <Text>Some sort of information here, perhaps?</Text>
            </>
          ),
        }}
        right={{
          children: (
            <>
              <DeprecatedHeading as="h2">Less important...</DeprecatedHeading>
              <Text>Some more information here, maybe?</Text>
            </>
          ),
        }}
        topLeftImage={{
          src: Triangle,
        }}
        bottomRightImage={{
          src: Stairs,
        }}
      />
    )}
  </Story>
</Canvas>
