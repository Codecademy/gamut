import { Pagination } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={Pagination}
  parameters={{
    subtitle: 'Pagination is a way to iterate through pages or content.',
    source: 'gamut',
    status: '',
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=8639%3A7128',
    },
  }}
  args={{
    chapterSize: 5,
    totalPages: 27,
    onChange: () => {},
    type: 'ellipsis',
  }}
/>

Smart Stephen words

## Usage

- Smart
- Stephen
- Words

<Canvas>
  <Story name="Pagination">{(args) => <Pagination {...args} />}</Story>
</Canvas>

<PropsTable story="Pagination" />

## Basic vs. Ellipsis

Most times we're basic, but somtimes we've got an ellipsis.

<Canvas>
  <Story
    name="ellipsis"
    args={{
      chapterSize: 5,
      totalPages: 27,
      onChange: () => {},
    }}
  >
    {Pagination.bind({})}
  </Story>
  <Story
    name="stroke"
    args={{
      chapterSize: 5,
      totalPages: 27,
      onChange: () => {},
      type: 'ellipsis',
    }}
  >
    {Pagination.bind({})}
  </Story>
</Canvas>

## Pagination Navigation

To do -- links. What do we do about them, etc.

<Canvas>
  <Story
    name="default size"
    args={{
      chapterSize: 5,
      totalPages: 27,
      type: 'ellipsis',
    }}
  >
    {Pagination.bind({})}
  </Story>
</Canvas>
