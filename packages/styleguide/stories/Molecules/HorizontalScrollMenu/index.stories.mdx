import { HorizontalScrollMenu } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { HorizontalScrollMenuExample } from './HorizontalScrollMenu.examples.tsx';

<Meta
  title={title}
  component={HorizontalScrollMenu}
  parameters={{ source: 'gamut', status: 'updating' }}
/>

The `HorizontalScrollMenu` can be used to have content scroll across the x-axis. Through use of the `IntersectionObserver` web api, this component will automatically set `aria-hidden` labels to `true` when content moves off the x-axis.

<Canvas>
  <Story name="Horizontal Scroll Menu">
    {() => <HorizontalScrollMenuExample />}
  </Story>
</Canvas>
