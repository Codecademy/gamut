import { HeaderContainer, HeaderTab } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  subcomponents={{ HeaderContainer, HeaderTab }}
  parameters={{
    subtitle: `Header navigation bar and tabs with configurable areas.`,
    status: 'deprecated',
    source: 'gamut',
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/TemYhRWOavsERWprDzRJCx',
    },
  }}
/>

<Canvas withSource="open">
  <Story name="Header">
    {() => (
      <HeaderContainer
        sections={{
          left: <HeaderTab>Hello</HeaderTab>,
          right: <HeaderTab>Right</HeaderTab>,
          after: <>After the rain has fallen</>,
        }}
      />
    )}
  </Story>
</Canvas>

<PropsTable story="Header" />
