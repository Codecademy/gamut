import { Column, FillButton, LayoutGrid, Text } from '@codecademy/gamut/src';
import { NumberBlocks } from '@codecademy/gamut-illustrations/src';
import { DropdownButton, EmptySection } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta
  title={title}
  component={EmptySection}
  parameters={{
    source: 'gamut-labs',
    subtitle: `A section that represents an empty state with an action for the user to take.`,
    status: 'updating',
  }}
  args={{
    bodyText: 'Body text',
    headingText: 'Heading text',
    illustration: NumberBlocks,
  }}
/>

The `children` can be either a `Button` or a `DropdownButton`.

## Illustration on the right (default)

<Canvas>
  <Story name="Illustration on the right">
    {(args) => (
      <EmptySection {...args}>
        <FillButton>Do Something</FillButton>
      </EmptySection>
    )}
  </Story>
</Canvas>

## Illustration on the left

<Canvas>
  <Story
    name="Illustration on the left"
    args={{ illustrationPosition: 'left' }}
  >
    {(args) => (
      <EmptySection {...args}>
        <FillButton>Do Something</FillButton>
      </EmptySection>
    )}
  </Story>
</Canvas>

## Dots stretch to the left (default)

<Canvas>
  <Story name="Dots stretch to the left">
    {(args) => (
      <EmptySection {...args}>
        <FillButton>Do Something</FillButton>
      </EmptySection>
    )}
  </Story>
</Canvas>

## Dots stretch to the right

<Canvas>
  <Story name="Dots stretch to the right" args={{ stretchDirection: 'right' }}>
    {(args) => (
      <EmptySection {...args}>
        <FillButton>Do Something</FillButton>
      </EmptySection>
    )}
  </Story>
</Canvas>

## Using a dropdown button

<Canvas>
  <Story
    name="Using a dropdown button"
    args={{
      stretchDirection: 'left',
    }}
  >
    {(args) => (
      <EmptySection {...args}>
        <DropdownButton
          dropdownItems={[
            { id: '1', text: 'Option 1' },
            { id: '2', text: 'Option 2' },
            { id: '3', text: 'Option 3' },
          ]}
        >
          Click me!
        </DropdownButton>
      </EmptySection>
    )}
  </Story>
</Canvas>

## How it looks with another section on the left

<Canvas>
  <Story
    name="Another section on the left"
    args={{
      stretchDirection: 'right',
    }}
  >
    {(args) => (
      <LayoutGrid gap={16}>
        <Column size={{ _: 12, sm: 4, md: 3 }}>
          <Text>{`<other section>`}</Text>
        </Column>
        <Column size={{ _: 12, sm: 8, md: 9 }}>
          <EmptySection {...args}>
            <DropdownButton
              dropdownItems={[
                { id: '1', text: 'Option 1' },
                { id: '2', text: 'Option 2' },
                { id: '3', text: 'Option 3' },
              ]}
            >
              Click me!
            </DropdownButton>
          </EmptySection>
        </Column>
      </LayoutGrid>
    )}
  </Story>
</Canvas>

## How it looks with another section on the right

<Canvas>
  <Story
    name="Another section on the right"
    args={{
      stretchDirection: 'left',
    }}
  >
    {(args) => (
      <LayoutGrid gap={16}>
        <Column size={{ _: 12, sm: 8, md: 9 }}>
          <EmptySection {...args}>
            <DropdownButton
              dropdownItems={[
                { id: '1', text: 'Option 1' },
                { id: '2', text: 'Option 2' },
                { id: '3', text: 'Option 3' },
              ]}
            >
              Click me!
            </DropdownButton>
          </EmptySection>{' '}
        </Column>
        <Column size={{ _: 12, sm: 4, md: 3 }}>
          <Text>{`<other section>`}</Text>
        </Column>
      </LayoutGrid>
    )}
  </Story>
</Canvas>

## Code Playground

Edit attributes and see what’s possible to do with this component or get the exact output you want.

<Canvas>
  <Story name="Playground">
    {(args) => (
      <EmptySection {...args}>
        <FillButton>Do Something</FillButton>
      </EmptySection>
    )}
  </Story>
</Canvas>

<PropsTable story="Playground" />
