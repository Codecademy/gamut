import { FillButton } from '@codecademy/gamut/src';
import { NumberBlocks } from '@codecademy/gamut-illustrations/src';
import { EmptySection } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={EmptySection}
  parameters={{
    source: 'gamut-labs',
    subtitle: `A section that represents an empty state with an action for the user to take.`,
    status: 'updating',
  }}
  args={{
    innerBGColor: 'white',
    bodyText: 'Body text',
    headingText: 'Heading text',
    illustration: NumberBlocks,
    children: <FillButton>Do Something</FillButton>,
  }}
/>

The `children` prop should take a `*Button` or a `*Button` that triggers another component to appear (like a dropdown, for example.)

The "innerBGColor" should match the background color of the page or section it's in.

## Illustration on the right (default)

<Canvas>
  <Story name="Illustration on the right">
    {(args) => <EmptySection {...args} />}
  </Story>
</Canvas>

## Illustration on the left

<Canvas>
  <Story
    name="Illustration on the left"
    args={{ illustrationPosition: 'left' }}
  >
    {(args) => <EmptySection {...args} />}
  </Story>
</Canvas>

## Stretch Direction on the left (default)

<Canvas>
  <Story name="Stretch Direction on the left">
    {(args) => <EmptySection {...args} />}
  </Story>
</Canvas>

## Stretch Direction on the right

<Canvas>
  <Story
    name="Stretch Direction on the right"
    args={{ stretchDirection: 'right' }}
  >
    {(args) => <EmptySection {...args} />}
  </Story>
</Canvas>

## Code Playground

Edit attributes and see what’s possible to do with this component or get the exact output you want.

<Canvas>
  <Story name="Playground">{(args) => <EmptySection {...args} />}</Story>
</Canvas>

<PropsTable story="Playground" />
