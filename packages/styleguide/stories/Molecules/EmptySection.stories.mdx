import { FillButton } from '@codecademy/gamut/src';
import { NumberBlocks } from '@codecademy/gamut-illustrations/src';
import { EmptySection, DropdownButton } from '@codecademy/gamut-labs/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import { PropsTable } from '@codecademy/storybook-addon-variance';

<Meta
  title={title}
  component={EmptySection}
  parameters={{
    source: 'gamut-labs',
    subtitle: `A section that represents an empty state with an action for the user to take.`,
    status: 'updating',
  }}
  args={{
    innerBGColor: 'white',
    bodyText: 'Body text',
    headingText: 'Heading text',
    illustration: NumberBlocks,
    children: <FillButton>Do Something</FillButton>,
  }}
/>

The `children` can be either a `Button` or a `DropdownButton`. You should set the "innerBGColor"
to match the background color of the page or section it's in.

## Illustration on the right (default)

<Canvas>
  <Story name="Illustration on the right">
    {(args) => <EmptySection {...args} />}
  </Story>
</Canvas>

## Illustration on the left

<Canvas>
  <Story
    name="Illustration on the left"
    args={{ illustrationPosition: 'left' }}
  >
    {(args) => <EmptySection {...args} />}
  </Story>
</Canvas>

## Dots stretch to the left (default)

<Canvas>
  <Story name="Dots stretch to the left">
    {(args) => <EmptySection {...args} />}
  </Story>
</Canvas>

## Dots stretch to the right

<Canvas>
  <Story name="Dots stretch to the right" args={{ stretchDirection: 'right' }}>
    {(args) => <EmptySection {...args} />}
  </Story>
</Canvas>

## Using a dropdown button

<Canvas>
  <Story
    name="Using a dropdown button"
    args={{
      stretchDirection: 'left',
      children: (
        <DropdownButton
          dropdownItems={[
            { id: '1', text: 'Option 1' },
            { id: '2', text: 'Option 2' },
            { id: '3', text: 'Option 3' },
          ]}
        >
          Click me!
        </DropdownButton>
      ),
    }}
  >
    {(args) => <EmptySection {...args} />}
  </Story>
</Canvas>

## Code Playground

Edit attributes and see what’s possible to do with this component or get the exact output you want.

<Canvas>
  <Story name="Playground">{(args) => <EmptySection {...args} />}</Story>
</Canvas>

<PropsTable story="Playground" />
