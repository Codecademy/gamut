import { Banner } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/dist/blocks';

<Meta
  title={title}
  component={Banner}
  parameters={{
    subtitle: `Full-width banner to indicate important messaging at the top of a page or section.`,
    status: 'volatile',
    source: 'gamut',
    figmaId: 'wTwIV8YfAgZ8eNpKSEd0Gn',
  }}
  args={{
    children: 'Happy Holidays! We have a Student discount for Codecademy Pro.',
  }}
/>

<Canvas>
  <Story name="Banner">{(args) => <Banner {...args} />}</Story>
</Canvas>

<Props story="Banner" />

## Variants

<Canvas isColumn>
  <Story name="Navy" args={{ children: 'Navy', variant: 'navy' }}>
    {(args) => <Banner {...args} />}
  </Story>
  <Story name="Yellow" args={{ children: 'Yellow', variant: 'yellow' }}>
    {(args) => <Banner {...args} />}
  </Story>
</Canvas>

## Call to Action

<Canvas isColumn>
  <Story name="Call to Action" args={{ cta: 'Learn more' }}>
    {(args) => <Banner {...args} />}
  </Story>
</Canvas>
