import { Accordion } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={Accordion}
  parameters={{
    subtitle: `A molecule consisting of a "top" section and an expandable body,
such as a "hint" section in the LE or a list of department jobs on a careers page.
`,
    status: 'current',
    source: 'gamut',
  }}
  args={{
    renderHeader: <div>Click me!</div>,
    renderExpanded: <div>Hidden treasure!</div>,
    ariaLabel: 'click me',
  }}
/>

<Canvas>
  <Story name="Accordion">{(args) => <Accordion {...args} />}</Story>
</Canvas>

<PropsTable story="Accordion" />

## Custom Text

<Canvas>
  <Story
    name="Custom text"
    args={{
      customText: {
        expanded: 'Hide details',
        collapsed: 'Show details',
      },
    }}
  >
    {(args) => <Accordion {...args} />}
  </Story>
</Canvas>

## Initially Expanded

<Canvas>
  <Story name="Expanded" args={{ initiallyExpanded: true }}>
    {(args) => <Accordion {...args} />}
  </Story>
</Canvas>
