import { InfoCircleIcon } from '@codecademy/gamut-icons/src';
import { AppHeaderLinkMobile } from '@codecademy/gamut-labs/src';
import { StyledBadge } from '@codecademy/gamut-labs/src/AppHeader/AppHeaderElements/AppHeaderLinkSections';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={AppHeaderLinkMobile}
  parameters={{
    layout: 'padded',
    status: 'updating',
    subtitle:
      'A styled Anchor with an optional icon, badge and top separator for use in AppHeaderLinkSections and the mobile menu.',
  }}
  args={{
    item: {
      id: 'link',
      icon: InfoCircleIcon,
      href: 'http://codecademy.com',
      text: 'Nav Link',
      trackingTarget: 'track-link',
      type: 'link',
    },
  }}
/>

<Canvas>
  <Story name="AppHeaderLinkMobile">
    {(args) => (
      <AppHeaderLinkMobile
        {...args}
        badge={
          <StyledBadge
            round
            variant="blue"
            fontFamily="accent"
            ml={8}
            pl={8}
            pr={8}
            height={16}
            alignSelf="center"
          >
            new
          </StyledBadge>
        }
      />
    )}
  </Story>
</Canvas>

<PropsTable story="AppHeaderLinkMobile" />
