import { Aside } from '@codecademy/gamut';
import { BookFlipPageIcon, StreakIcon } from '@codecademy/gamut-icons';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={Aside}
  parameters={{
    subtitle: 'An aside component with a list and some buttons',
    source: 'gamut',
    status: 'updating',
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/2Hh7PYIPWLf4ZmWW4yKTfu/Gamut-Example---Aside-Component?node-id=2%3A2',
    },
  }}
  args={{
    header: 'My Header Text',
    headerLevel: 'h2',
    subtitle: 'My subtitle',
    listItems: [
      { icon: StreakIcon, numberOfItems: 42, id: 'Blue' },
      { icon: BookFlipPageIcon, numberOfItems: 74, id: 'Red' },
    ],
    primaryButton: { children: 'Primary', onClick: () => {} },
    secondaryButton: { children: 'Secondary', onClick: () => {} },
  }}
/>

<Canvas>
  <Story name="Aside">{(args) => <Aside {...args} />}</Story>
</Canvas>

<PropsTable story="Aside" />

## Patterns

some Patterns

## Variants

My variants

<Canvas>
  <Story args={{ variant: 'yellow' }} name="Aside Secondary">
    {(args) => <Aside {...args} />}
  </Story>
</Canvas>
