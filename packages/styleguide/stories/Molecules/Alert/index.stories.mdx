import { Alert } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Meta, Story } from '@storybook/addon-docs/blocks';


<Meta
  title={title}
  component={Alert}
  parameters={{
    subtitle:
      'An alert displays an important, succinct message, and provides actions for users to address (or dismiss the alert). It requires a user action to be dismissed.',
    status: 'current',
    source: 'gamut',
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/TemYhRWOavsERWprDzRJCx',
    },
    padded: true,
  }}
  args={{
    type: 'general',
    placement: 'floating',
    cta: {
      children: 'Learn More',
    },
    children:
      'You have been logged out due to inactivity. Click the button to log in again.',
  }}
/>

{/* 'You have been logged out due to inactivity. Click the button to log in again.You have been logged out due to inactivity. Click the button to log in again.You have been logged out due to inactivity. Click the button to log in again.', */}

<Story name="Usage guidelines">{(args) => <Alert {...args} />}</Story>

