import cx from 'classnames';
import { withKnobs } from '@storybook/addon-knobs';
import { startCase } from 'lodash';
import { StoryStatus } from '../../Templating/Blocks';
import { Meta, Props, Preview, Story } from '@storybook/addon-docs/dist/blocks';
import {
  LayoutGrid,
  Column,
  VisualTheme,
  Container,
  Heading,
} from '@codecademy/gamut/src';
import {
  brandColors,
  colors,
  deprecatedColors,
  editorColors,
  deprecatedGamutColors,
  platformColors,
} from '@codecademy/gamut-styles/utils/variables';
import { excludedColors, baseColors } from './constants';
import styles from './styles.module.scss';
import { Swatch, SwatchPalette, parseCamelCase, objectKeys } from './Elements';

<Meta
  title="Core|Foundations/Colors"
  component="Colors"
  decorators={[withKnobs]}
/>

# Colors

These are the color palettes used by both design and engineering.

## Active Colors

<StoryStatus status="ready" />

### Brand Colors

Brand color atoms we select from in creating designs. All colors seen on
Codecademy properties should adhere to these. Remember your accessibility
contrast requirements too!

The most visually distinct colors associated with our brand are
unsurprisingly referred to as "brand" colors. These are commonly used on
buttons and as vibrant background colors.

<Preview withSource="none" columns={1}>
  <Story name="Brand">
    <LayoutGrid className={styles.swatchesContainer} rowGap="md" columnGap="md">
      {objectKeys(brandColors).map((color) => (
        <Column key={color} size={{ xs: 6, sm: 3 }}>
          <Container flex={false}>
            <Heading as="h2" fontSize="xs">
              Brand {startCase(color)}
            </Heading>
            <Swatch
              name={`$color-${parseCamelCase(color)}`}
              hex={brandColors[color]}
            />
          </Container>
        </Column>
      ))}
    </LayoutGrid>
  </Story>
  <Story name="Base">
    <LayoutGrid className={styles.swatchesContainer} rowGap="md" columnGap="md">
      {objectKeys(baseColors).map((color) => (
        <Column key={color} size={{ xs: 6, sm: 3 }}>
          <Container flex={false}>
            <Heading as="h2" fontSize="xs">
              {parseCamelCase(`color-${color}`)}
            </Heading>
            <Swatch
              name={`$color-${parseCamelCase(color)}`}
              hex={baseColors[color]}
            />
          </Container>
        </Column>
      ))}
    </LayoutGrid>
  </Story>
  <Story name="Rest">
    <LayoutGrid className={styles.swatchesContainer} rowGap="md" columnGap="md">
      {objectKeys(colors)
        .filter((color) => !excludedColors.includes(color))
        .map((color) => (
          <Column key={color} size={{ xs: 6, sm: 3 }}>
            <Container flex={false}>
              <Heading as="h2" fontSize="xs">
                {parseCamelCase(`color-${color}`)}
              </Heading>
              <SwatchPalette
                variablePrefix={`color-${color}`}
                data={colors[color]}
              />
            </Container>
          </Column>
        ))}
    </LayoutGrid>
  </Story>
</Preview>

### Platform Colors (Learning Environment)

Our Learning Environment ("LE") is, for now, an exception to the rule
about containing only brand and grid colors. It uses a selection of muted
grayish purples and a few mid-range minty greens.

<Preview withSource="none" columns={1}>
  <Story name="Platform ">
    <LayoutGrid className={styles.swatchesContainer} rowGap="md" columnGap="md">
      {objectKeys(platformColors).map((color) => (
        <Column key={color} size={{ xs: 6, sm: 3 }}>
          <Container flex={false}>
            <Heading as="h2" fontSize="xs">
              Platform {startCase(color)}
            </Heading>
            <SwatchPalette
              variablePrefix={`platform-${color}`}
              data={platformColors[color]}
            />
          </Container>
        </Column>
      ))}
    </LayoutGrid>
  </Story>
</Preview>

### Editor Colors

The LE's code editor uses its own colors for text.

<Preview className="dark">
  <Story name="Editor">
    <LayoutGrid className={styles.swatchesContainer} rowGap="md" columnGap="md">
      {objectKeys(editorColors).map((color) => (
        <Column key={color} size={{ xs: 6, sm: 3 }}>
          <Container flex={false}>
            <Heading
              as="h2"
              fontSize="xs"
              className={cx(styles.heading, styles.headingDark)}
            >
              Editor {startCase(color)}
            </Heading>
            <Swatch
              name={`color-editor-${parseCamelCase(color)}`}
              hex={editorColors[color]}
            />
          </Container>
        </Column>
      ))}
    </LayoutGrid>
  </Story>
</Preview>

## Deprecated

### Gamut

We used to have a very different color palette. Please do not use these
anymore.

If you come across a color on the site while updating an older area and see
that it uses one of these, try to replace it with the closest color from or
new palette.

We may delete these as we find that consumers do not use them.

<Preview withSource="none" columns={1}>
  <Story name="Gamut Deprecated Base">
    <LayoutGrid className={styles.swatchesContainer} rowGap="md" columnGap="md">
      <Column size={{ xs: 6, sm: 3 }}>
        <Container flex={false}>
          <Heading as="h2" fontSize="xs">
            Deprecated Gamut Base Colors
          </Heading>
          <SwatchPalette
            data={deprecatedGamutColors.base}
            variablePrefix="deprecated-gamut"
          />
        </Container>
      </Column>
    </LayoutGrid>
  </Story>
  <Story name="Gamut Deprecated All">
    <LayoutGrid className={styles.swatchesContainer} rowGap="md" columnGap="md">
      {objectKeys(deprecatedGamutColors.swatches).map((color) => (
        <Column key={color} size={{ xs: 6, sm: 3 }}>
          <Container flex={false}>
            <Heading as="h2" fontSize="xs">
              {parseCamelCase(`deprecated-gamut-${color}`)}
            </Heading>
            <SwatchPalette
              data={deprecatedGamutColors.swatches[color]}
              variablePrefix={`deprecated-gamut-${color}`}
            />
          </Container>
        </Column>
      ))}
    </LayoutGrid>
  </Story>
</Preview>

### Portal

Similar to the deprecated Gamut colors, these ones are an old palette from
the Portal.

<Preview withSource="none" columns={1}>
  <Story name="Portal Deprecated Base">
    <LayoutGrid className={styles.swatchesContainer} rowGap="md" columnGap="md">
      <Column size={{ xs: 6, sm: 3 }}>
        <Container flex={false}>
          <Heading as="h2" fontSize="xs">
            Deprecated portal base colors
          </Heading>
          <SwatchPalette
            data={deprecatedColors.portal}
            variablePrefix="deprecated"
          />
        </Container>
      </Column>
    </LayoutGrid>
  </Story>
  <Story name="Portal Deprecated All">
    <LayoutGrid className={styles.swatchesContainer} rowGap="md" columnGap="md">
      {objectKeys(deprecatedColors.swatches).map((color) => (
        <Column key={color} size={{ xs: 6, sm: 3 }}>
          <Container flex={false}>
            <Heading as="h2" fontSize="xs">
              {parseCamelCase(color)}
            </Heading>
            <SwatchPalette
              data={deprecatedColors.swatches[color]}
              variablePrefix={`deprecated-swatches-${color}`}
            />
          </Container>
        </Column>
      ))}
    </LayoutGrid>
  </Story>
</Preview>
