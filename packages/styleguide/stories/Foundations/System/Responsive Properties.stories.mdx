import docs from '@codecademy/gamut-system/docs/responsive.md';
import title from '@codecademy/macros/lib/title.macro';
import { Description, Meta } from '@storybook/addon-docs/blocks';

import { formatExternalMarkdown } from '~styleguide/blocks';

<Meta
  title={title}
  parameters={{
    subtitle:
      'All system props accept a syntax to generate responsive styles on a per prop basis',
    source: 'gamut-styles',
    status: 'updating',
  }}
/>

<Description>{formatExternalMarkdown(docs)}</Description>
