import docs from '@codecademy/gamut-system/docs/properties.md';
import title from '@codecademy/macros/lib/title.macro';
import { Description, Meta } from '@storybook/addon-docs/blocks';

import { formatExternalMarkdown } from '~styleguide/blocks';

<Meta
  title={title}
  parameters={{
    subtitle:
      'Reusable CSS-in-JS props with predictable behaviors and a consistent API for responsive CSS.',
    source: 'gamut-styles',
    status: 'updating',
  }}
/>

<Description>{formatExternalMarkdown(docs)}</Description>
