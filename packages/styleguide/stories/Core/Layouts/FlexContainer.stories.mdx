import { Meta, Props, Preview, Story } from '@storybook/addon-docs/dist/blocks';
import { StoryStatus } from '../../../Blocks';
import { Box, Flex } from '@codecademy/gamut/src';
import { BoxElement, defaultGridProps } from './Elements';
import { select, boolean, number } from '@storybook/addon-knobs';

<Meta title="Layouts/Box" component={Box} subcomponents={{ Flex }} />

# Container

<StoryStatus status="stable" />

A flex box primitive that allows you create markup configurable flex containers

<Preview>
  <Story
    name="Box"
    withSource="open"
    args={{
      display: 'flex',
      flexDirection: 'column',
    }}
  >
    {(args) => {
      return (
        <Flex {...args}>
          <Box style={{ padding: 10, background: 'blue', color: 'white' }}>
            New Container Item 1
          </Box>
          <Flex row style={{ padding: 10, background: 'blue', color: 'white' }}>
            <Box style={{ padding: 10, background: 'blue', color: 'white' }}>
              New Container Item 2
            </Box>
            <Box style={{ padding: 10, background: 'blue', color: 'white' }}>
              New Container Item 2
            </Box>
          </Flex>
        </Flex>
      );
    }}
  </Story>
</Preview>

<Props of={Box} />
