import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { StoryStatus } from '~styleguide/blocks';
import { Heading, Text } from '@codecademy/gamut';
import {
  Box,
  Grid,
  Flex,
  Logo,
  Tag,
  patterns,
} from '@codecademy/gamut-labs/src';
import { colors } from '@codecademy/gamut-styles/src';
import { Box as ExampleBox } from '~styleguide/blocks';
import { boxTypes } from './argTypes';
import { DottedBox, MatrixBox, DiagonalBox } from './Test';

<Meta title="Layouts/Box" component={Box} argTypes={boxTypes} />

# Box

<StoryStatus status="volatile" />

A flex box primitive that allows you create markup configurable flex containers

<Canvas>
  <Story
    name="Box"
    args={{
      children: 'Hello world I am a box',
      padding: 16,
      bordered: true,
    }}
  >
    {(args) => <Box {...args} />}
  </Story>
</Canvas>

<Props story="Box" />

## Composition Example

<Canvas>
  <Story name="GridCard Example" args={{ bordered: true }}>
    {(args) => (
      <Grid bordered>
        <Grid
          padding={16}
          gap={8}
          ratio="max-content:max-content-max-content:1fr"
        >
          <Grid gap={8} direction="column">
            <Logo height={20} type="proAlt" />
            <Text fontSize="sm">Course</Text>
          </Grid>
          <Heading as="h3" fontSize="md" hideMargin>
            This is a course card
          </Heading>
          <Text fontSize="sm">
            Beginner friendly |{' '}
            <Text as="strong" fontSize="sm">
              70
            </Text>{' '}
            Lessons
          </Text>
        </Grid>
        <Flex alignContent="flex-end" justifyContent="flex-end">
          <Tag background="pink">Has a final project</Tag>
        </Flex>
      </Grid>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story name="Drop">
    {(args) => (
      <Grid padding={24} gap={48}>
        <Grid
          direction="column"
          gap={48}
          justify="center"
          align="center"
          ratio="1fr:1fr:1fr"
        >
          <DiagonalBox bordered padding={48}>
            Test
          </DiagonalBox>
          <DottedBox bordered padding={48}>
            Test
          </DottedBox>
          <MatrixBox bordered padding={48}>
            Test
          </MatrixBox>
        </Grid>
        <Grid
          direction="column"
          gap={48}
          justify="center"
          align="center"
          ratio="1fr:1fr:1fr"
        >
          <DiagonalBox bordered padding={48}>
            Test
          </DiagonalBox>
          <DottedBox background="navy" bordered padding={48}>
            Test
          </DottedBox>
          <MatrixBox bordered background="yellow" padding={48}>
            Test
          </MatrixBox>
        </Grid>
      </Grid>
    )}
  </Story>
</Canvas>
