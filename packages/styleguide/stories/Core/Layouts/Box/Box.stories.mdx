import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { StoryStatus } from '~styleguide/blocks';
import { SystemBox } from '@codecademy/gamut-labs/src';

<Meta title="Layouts/SystemBox (Labs)" component={SystemBox} />

# Box

<StoryStatus status="volatile" />

A primitive box component.

<Canvas>
  <Story
    name="SystemBox"
    args={{
      children: 'Hello world I am a box',
    }}
  >
    {(args) => (
      <>
        <SystemBox variant="primary">I am a primary</SystemBox>
        <SystemBox variant="secondary">I am a secondary</SystemBox>
      </>
    )}
  </Story>
</Canvas>

<Props story="SystemBox" />
