import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { select, boolean } from '@storybook/addon-knobs';
import {
  CardShell,
  CardBody,
  CardFooter,
  Heading,
  Text,
} from '@codecademy/gamut/src';

<Meta
  title="Atoms/Card (Deprecated)"
  component={CardShell}
  subcomponents={{ CardBody, CardFooter }}
  parameters={{
    subtitle: `A set of components for displaying a consistently spaced raised card.`,
    status: 'deprecated',
    source: 'gamut',
  }}
/>

<Canvas>
  <Story name="Card">
    {(args) => (
      <CardShell hoverShadow={boolean('shell.hoverShadow', false)}>
        <CardBody standardPadding={boolean('body.standardPadding', true)}>
          <Heading as="h3">Card Body</Heading>
          <Text>This is some body text</Text>
          <Text>Blah blah blurgha blurgha</Text>
        </CardBody>
        <CardFooter
          align={select('Align', ['left', 'center', 'right'], 'left')}
          border={select('Border', ['dashed', 'solid', 'none'], 'none')}
          flex={boolean('Flex', true)}
          standardPadding={boolean('Standard Padding', true)}
          standardHeight={boolean('Standard Height', true)}
        >
          <React.Fragment>Footer Text</React.Fragment>
        </CardFooter>
      </CardShell>
    )}
  </Story>
</Canvas>

<Props of={CardShell} />
