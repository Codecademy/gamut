import { Meta, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { StoryStatus, PropsTable } from '~styleguide/blocks';
import { Card } from '@codecademy/gamut-labs/src';

<Meta
  title="Atoms/Card (Labs)"
  component={Card}
  args={{
    children: 'Hello world I am a Card',
    padding: 16,
  }}
/>

# Card

<StoryStatus status="volatile" />

A flex box primitive that allows you create markup configurable flex containers

<Canvas>
  <Story name="Card">{(args) => <Card {...args} />}</Story>
</Canvas>

<PropsTable story="Card" />

## Variants

We provide 3 style variants for Card.

<Canvas>
  <Story name="White" args={{ variant: 'white' }}>
    {(args) => <Card {...args} />}
  </Story>
  <Story name="Yellow" args={{ variant: 'yellow' }}>
    {(args) => <Card {...args} />}
  </Story>
  <Story name="Navy" args={{ variant: 'navy' }}>
    {(args) => <Card {...args} />}
  </Story>
</Canvas>
