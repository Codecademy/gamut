import { Meta, Props, Story, Canvas } from '@storybook/addon-docs/dist/blocks';
import { StoryStatus } from '~styleguide/blocks';
import {
  Button,
  ButtonBase,
  RadialProgress,
  Spinner,
} from '@codecademy/gamut/src';
import { action } from '@storybook/addon-actions';
import {
  themes,
  brandThemeKeys,
  statusThemeKeys,
  deprecatedThemeKeys,
} from './helpers';

<Meta
  title="Atoms/Button"
  component={Button}
  argTypes={{
    theme: {
      control: {
        type: 'select',
        options: [...brandThemeKeys, ...statusThemeKeys],
      },
    },
  }}
  args={{ theme: 'brand-blue', children: 'Submit' }}
/>

# Buttons

<StoryStatus status="stable" />

Primary versions of buttons and links. Approximately all focusable,
interactive elements should use some form of a `Button` (if
they look like this) or `ButtonBase` (if they have custom
styles).

Prefer `brand-purple` for significant actions such as Pro signups. Prefer `brand-blue` for generic actions.

Play with this button, if you&apos;d like!

<Canvas>
  <Story name="Button" args={{ children: 'Submit' }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

<Props story="Button" />

## Themes

There are several theme sets you can use with `Button`

### Brand

<Canvas columns={1}>
  <Story name="Brand Buttons" args={{ style: { margin: '1rem' } }}>
    {(args) =>
      brandThemeKeys.map((theme) => (
        <Button key={`${theme}`} {...args} theme={theme}>
          {theme}
        </Button>
      ))
    }
  </Story>
</Canvas>

### Status

We also have feedback specific button themes best paired with `Alert`

<Canvas>
  <Story name="Status Buttons" args={{ style: { margin: '1rem' } }}>
    {(args) =>
      statusThemeKeys.map((theme) => (
        <Button key={`${theme}`} {...args} theme={theme}>
          {theme}
        </Button>
      ))
    }
  </Story>
</Canvas>

### Deprecated

We also have legacy button colors floating around. Do not use these.

<Canvas>
  <Story name="Deprecated" args={{ style: { margin: '1rem' } }}>
    {(args) =>
      deprecatedThemeKeys.map((theme) => (
        <Button key={`${theme}`} {...args} theme={theme}>
          {theme}
        </Button>
      ))
    }
  </Story>
</Canvas>

## Button States

Common variants of buttons you might come across.

<Canvas>
  <Story name="Disabled " args={{ disabled: true, children: 'Disabled' }}>
    {(args) => <Button {...args} />}
  </Story>
  <Story
    name="Loading"
    args={{
      children: (
        <>
          <Spinner style={{ marginRight: '15px' }} /> Loading...
        </>
      ),
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

## Variants

### Link

You may also configure a button to behave purely like a link

<Canvas isColumn>
  <Story
    name="Link Button"
    args={{ link: true, href: '#', children: 'link', style: null }}
  >
    {(args) => (
      <>
        This is an example of a <Button {...args} />
      </>
    )}
  </Story>
</Canvas>

### Visual Variants

Buttons might also be `round` to indicate rounded corners. These tend to
be larger, advertise-y placements.

<Canvas isColumn>
  <Story name="Round" args={{ round: true, children: 'Round' }}>
    {(args) => <Button {...args} />}
  </Story>
  <Story name="Flat" args={{ flat: true, href: '#', children: 'Flat' }}>
    {(args) => <Button {...args} />}
  </Story>
  <Story
    name="Outline"
    args={{ href: '#', outline: true, children: 'Outline' }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Size Variants

<Canvas isColumn>
  <Story name="Large" args={{ size: 'large', children: 'Large' }}>
    {(args) => <Button {...args} />}
  </Story>
  <Story name="Small" args={{ size: 'small', children: 'Small' }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Display Variants

<Canvas isColumn>
  <Story name="Square" args={{ square: true, children: 'X' }}>
    {(args) => <Button {...args} />}
  </Story>
  <Story
    name="Fit Text"
    args={{
      fitText: true,
      children: 'Some Text to Fit, that might be very long',
    }}
  >
    {(args) => <Button {...args} />}
  </Story>
  <Story name="Block" args={{ block: true, children: 'Block' }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

### Text Variants

<Canvas>
  <Story name="Caps" args={{ caps: true }}>
    {(args) => <Button {...args} />}
  </Story>
</Canvas>
