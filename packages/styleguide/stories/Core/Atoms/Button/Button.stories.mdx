import { action } from '@storybook/addon-actions';
import { Meta, Props, Story, Canvas } from '@storybook/addon-docs/dist/blocks';
import {
  CTAButton,
  FillButton,
  IconButton,
  StrokeButton,
  TextButton,
  VisualTheme,
} from '@codecademy/gamut/src';
import { SearchIcon } from '@codecademy/gamut-icons/src';
import { select, text, boolean } from '@storybook/addon-knobs';

import { StoryStatus } from '~styleguide/blocks';

<Meta
  args={{
    children: 'Click Me',
    disabled: false,
    mode: VisualTheme.LightMode,
    href: '',
  }}
  argTypes={{
    mode: {
      control: {
        type: 'select',
        options: [VisualTheme.DarkMode, VisualTheme.LightMode],
      },
    },
  }}
  title="Atoms/Button"
/>

# Buttons

<StoryStatus status="stable" />

We have several versions of buttons that you can use.
Each supports a _light_ and _dark_ mode.

## CTA Button

<Canvas>
  <Story name="CTAButton">{(args) => <CTAButton {...args} />}</Story>
</Canvas>

<Props story="CTAButton" />

## Fill Button

<Canvas>
  <Story name="FillButton">{(args) => <FillButton {...args} />}</Story>
</Canvas>

<Props story="FillButton" />

## Stroke Button

<Canvas>
  <Story name="StrokeButton">{(args) => <StrokeButton {...args} />}</Story>
</Canvas>

<Props story="StrokeButton" />

## Text Button

<Canvas>
  <Story name="TextButton">{(args) => <TextButton {...args} />}</Story>
</Canvas>

<Props story="TextButton" />

## Icon Button

<Canvas>
  <Story name="IconButton">
    {(args) => <IconButton icon={SearchIcon} {...args} />}
  </Story>
</Canvas>

<Props story="IconButton" />
