import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { values } from 'lodash';
import { InfoCircleIcon } from '@codecademy/gamut-icons/src';
import { StoryStatus } from '~styleguide/blocks';
import {
  ToolTip,
  ToolTipPosition,
  Container,
  VisualTheme,
} from '@codecademy/gamut/src';

<Meta
  title="Atoms/ToolTip"
  component={ToolTip}
  decorators={[
    (Story) => (
      <Container justify="center" align="center" style={{ margin: '2rem 0' }}>
        <Story />
      </Container>
    ),
  ]}
  argTypes={{
    target: {
      control: { disable: true },
    },
    theme: {
      table: {
        defaultValue: { summary: VisualTheme.LightMode },
      },
    },
    position: {
      table: {
        defaultValue: { summary: ToolTipPosition.TopRight },
      },
    },
  }}
  args={{
    focusable: true,
    id: 'tooltip',
    theme: VisualTheme.LightMode,
    position: ToolTipPosition.TopRight,
  }}
/>

# ToolTip

<StoryStatus status="stable" />

Small piece of extra information triggered on the focus or hover of a button or icon,
such as extra requirements for a form surfaced from an informative (i) icon. They
dissapear when the focus or hover are no longer active.

Tooltips cannot contain clickable elements (if your solution
requires clickable elements, we suggest using a “Popover” instead) - Great for
surfacing additional information that may or may not be relevant _without_ cluttering
the core interface.

<Canvas isColumn>
  <Story name="ToolTip">
    {(args) => (
      <ToolTip {...args} target={<InfoCircleIcon size={24} />}>
        Like the term fortnight, a term for a two week period
      </ToolTip>
    )}
  </Story>
</Canvas>

<Props story="ToolTip" />
