import { Meta, Props, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { StoryStatus } from '~styleguide/blocks';
import { Box } from '@codecademy/gamut-labs/src';
import { boxTypes } from './argTypes';

<Meta title="Core/Atoms/Box (Labs)" component={Box} argTypes={boxTypes} />

# Box

<StoryStatus status="volatile" />

A flex box primitive that allows you create markup configurable flex containers

<Canvas>
  <Story
    name="Box"
    args={{
      children: 'Hello world I am a box',
      padding: 16,
      bordered: true,
    }}
  >
    {(args) => <Box {...args} />}
  </Story>
</Canvas>

<Props story="Box" />
