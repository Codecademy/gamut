import { Meta, Props, Preview, Story } from '@storybook/addon-docs/dist/blocks';
import { action } from '@storybook/addon-actions';
import { boolean } from '@storybook/addon-knobs';
import { StoryStatus } from '~styleguide/blocks';
import { Toggle } from '@codecademy/gamut/src';

<Meta title="Atoms/Toggle" component={Toggle} />

# Toggle

<StoryStatus status="stable" />

Controlled atom component indicating a user-selectable boolean value.
Receives its `checked` prop from its parent. This is the default theme and size.

<Preview>
  <Story name="Toggle">
    <Toggle
      checked={boolean('checked', false)}
      label="Toggle"
      onChange={action('Change')}
    />
  </Story>
</Preview>

## Alternative Toggle

This is the small size and brand purple theme.

<Preview>
  <Story name="Alternative Toggle">
    <Toggle
      checked={boolean('checked', false)}
      label="Toggle"
      onChange={action('Change')}
      theme="purple"
      size="sm"
    />
  </Story>
</Preview>

<Props of={Toggle} />
