import { Meta, Canvas, Story } from '@storybook/addon-docs/dist/blocks';
import { PropsTable } from '~styleguide/blocks';
import { Text } from '@codecademy/gamut-labs/src';

<Meta
  title="Typography/Text"
  component={Text}
  parameters={{ source: 'gamut-labs' }}
/>

This is a single primitive to handle all `<p>` and
`<span>` tags with defined size intervals.

<Canvas>
  <Story name="Text" args={{ children: 'Hello World' }}>
    {(args) => <Text {...args} />}
  </Story>
</Canvas>

## Props

<PropsTable story="Text" defaultGroups={['typography']} />
