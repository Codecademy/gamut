import {
  ConnectedCheckbox,
  ConnectedForm,
  ConnectedFormGroup,
  ConnectedSelect,
  SubmitButton,
} from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { HEY, HEYagain, TestOne } from './examples';

<Meta
  title={title}
  component={ConnectedForm}
  parameters={{
    subtitle: 'The special sauce to creating modular forms',
    source: 'gamut',
    status: 'current',
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=1189%3A0',
    },
  }}
/>

<Canvas>
  <Story
    name="ConnectedForm"
    args={{ validation: 'onSubmit', resetOnSubmit: true }}
  >
    {() => (
      <ConnectedForm
        onSubmit={(values) => console.log(values)}
        defaultValues={{ 'this-field': true, 'that-field': 'three' }}
      >
        <ConnectedFormGroup
          name="this-field"
          label="hi"
          size="large"
          spacing="tight"
          field={{
            component: ConnectedCheckbox,
            label: 'check me out',
          }}
        />
        <ConnectedFormGroup
          name="that-field"
          label="hey"
          size="large"
          field={{
            component: ConnectedSelect,
            validation: {
              pattern: {
                value: /^(?:(?!zero).)*$/,
                message: 'zero to hero',
              },
            },
            options: ['zero', 'one', 'two', 'three'],
          }}
        />
        <SubmitButton mt={8}>submit!!!</SubmitButton>
      </ConnectedForm>
    )}
  </Story>
</Canvas>

<PropsTable of={ConnectedForm} />

## Inputs + Usage

<TestOne />

<HEY />

## useFormState

<HEYagain />

### Dynamic Fields + Watch

### Add some documenation around onChange
