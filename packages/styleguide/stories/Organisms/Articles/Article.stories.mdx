import { Article } from '@codecademy/gamut-labs';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';

import { PropsTable } from '~styleguide/blocks';

import { AppDecorator, mockArticle } from './utils';

<Meta
  title={title}
  component={Article}
  parameters={{
    subtitle: 'Articles',
    source: 'gamut-labs',
    status: '',
    figmaId: '',
    viewMode: 'canvas',
    layout: 'fullscreen',
  }}
  decorators={[(Story) => <AppDecorator>{Story()}</AppDecorator>]}
  args={mockArticle}
/>

<Canvas>
  <Story name="Article">{(args) => <Article {...args} />}</Story>
</Canvas>

<PropsTable story="Article" />
