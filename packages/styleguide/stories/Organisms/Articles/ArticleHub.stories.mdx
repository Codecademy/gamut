import { ArticleHub } from '@codecademy/gamut-labs';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';

import { PropsTable } from '~styleguide/blocks';

import { AppDecorator, articles } from './utils';

<Meta
  title={title}
  component={ArticleHub}
  parameters={{
    subtitle: 'ArticleHub',
    source: 'gamut-labs',
    status: '',
    figmaId: '',
    viewMode: 'canvas',
    layout: 'fullscreen',
  }}
  decorators={[(Story, args) => <AppDecorator>{Story()}</AppDecorator>]}
  args={{ articles }}
/>

<Canvas>
  <Story name="Article">{(args) => <ArticleHub {...args} />}</Story>
</Canvas>

<PropsTable story="Article" />
