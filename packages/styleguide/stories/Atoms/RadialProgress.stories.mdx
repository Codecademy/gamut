import { Box, RadialProgress, Video } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={RadialProgress}
  parameters={{
    subtitle: 'An SVG dial for displaying progress or percentages',
    source: 'gamut',
    status: 'deprecated',
  }}
  args={{
    strokeLinecap: 'round',
    strokeWidth: 10,
    value: 30,
    size: 120,
  }}
/>

A circular display of progress meant to convey a
percentage out of a whole.

<Canvas>
  <Story name="RadialProgress">{(args) => <RadialProgress {...args} />}</Story>
</Canvas>

<PropsTable story="RadialProgress" />

## Children

Optionally, children may be provided to be displayed in the center of the circle.
This is useful for conveying a textual description of progress, such as a percentage of X/Y ratio.

<Canvas>
  <Story name="Children" args={{ children: '75%', value: 75, size: 60 }}>
    {(args) => <RadialProgress {...args} />}
  </Story>
</Canvas>

## Animating

Since `value` can be a `number[]`, given an initial value, end value, and duration, these will animate
smoothly upon being rendered.

Example: `<RadialProgress value={[0, 100]} duration={5000} {...args} />`

Check out the video example below.

<Canvas>
  <Story name="Animating">
    <Box style={{ maxHeight: '190px', maxWidth: '190px' }}>
      <Video
        videoUrl="https://i.imgur.com/115O6iY.mp4"
        title="RadialProgress animation example"
        loop
        autoplay
        controls={false}
      />
    </Box>
  </Story>
</Canvas>
