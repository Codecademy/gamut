import { TextArea } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={TextArea}
  parameters={{
    subtitle: 'A `<textarea />` component for long form text entry',
    source: 'gamut',
    status: 'current',
  }}
  args={{
    htmlFor: 'example-input',
    name: 'example-input',
    rows: 4,
  }}
/>

<Canvas>
  <Story name="TextArea" args={{ defaultValue: 'Some text' }}>
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>

<PropsTable story="TextArea" />

### Input States

<Canvas>
  <Story name="Disabled" args={{ defaultValue: 'Disabled', disabled: true }}>
    {(args) => <TextArea {...args} />}
  </Story>
  <Story name="Error" args={{ defaultValue: 'Error', error: true }}>
    {(args) => <TextArea {...args} />}
  </Story>
  <Story
    name="Placeholder"
    args={{ placeholder: 'Placeholder', defaultValue: undefined }}
  >
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>

### Character count

<Canvas>
  <Story name="Character Count" args={{ characterMax: 60 }}>
    {(args) => <TextArea {...args} />}
  </Story>
  <Story
    name="Characters Exceed Max Error"
    args={{
      characterMax: 60,
      defaultValue:
        'Error state when the maximum characters value has been exceeded',
    }}
  >
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>
