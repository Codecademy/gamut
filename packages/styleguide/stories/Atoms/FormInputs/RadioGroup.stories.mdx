import { Radio, RadioGroup } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';

import { PropsTable } from '~styleguide/blocks';

export const exampleRadios = [{ id: '1' }, { id: '2' }];

<Meta
  title={title}
  component={RadioGroup}
  parameters={{
    subtitle: 'A standard radio input for handling boolean values',
    source: 'gamut',
    status: 'stable',
  }}
  args={{
    htmlFor: 'example-radio',
    label: 'A radio Label',
    name: 'example-radio',
    children: exampleRadios.map(({ id }) => (
      <Radio key={id} name={id} value={id} label={`Radio ${id}`} />
    )),
  }}
  argTypes={{
    children: {
      control: { disable: true },
    },
  }}
/>

<Canvas>
  <Story name="RadioGroup">{(args) => <RadioGroup {...args} />}</Story>
</Canvas>

<PropsTable story="RadioGroup" />
