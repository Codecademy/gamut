import { Box,InfoTip,Radio } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={Radio}
  parameters={{
    subtitle:
      'A `<input type="radio" />` element to be used with a `<RadioGroup />` component',
    source: 'gamut',
    status: 'current',
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=1189%3A0',
    },
  }}
  args={{
    htmlFor: 'example-radio',
    label: 'Option 1',
    name: 'example-radio',
  }}
/>

<Canvas>
  <Story name="Radio">{(args) => <Radio {...args} />}</Story>
</Canvas>

<PropsTable story="Radio" />

### Input States

<Canvas>
  <Story
    name="Disabled"
    args={{
      htmlFor: 'example-disabled',
      name: 'example-disabled',
      label: 'Disabled',
      disabled: true,
    }}
  >
    {(args) => <Radio {...args} />}
  </Story>
  <Story
    name="Disabled"
    args={{
      htmlFor: 'example-disabled-checked',
      name: 'example-disabled-checked',
      label: 'Disabled and Checked',
      disabled: true,
      checked: true,
    }}
  >
    {(args) => <Radio {...args} />}
  </Story>
  <Story
    name="Error"
    args={{
      htmlFor: 'example-error',
      label: 'Error',
      name: 'example-error',
      error: true,
    }}
  >
    {(args) => <Radio {...args} />}
  </Story>
</Canvas>

### Custom labels

The label is a ReactNode, making it customizable outside of a standard text label.

<Canvas>
  <Story
    name="Custom labels"
    args={{
      htmlFor: 'example-custom',
      name: 'example-custom',
      label: (
        <>
          Option with infotip
          <Box pl={8}>
            <InfoTip info="More info!" emphasis="high"/>
          </Box>
        </>
      ),
    }}
  >
    {(args) => (
      <Box height="5rem" bg="beige" p={5}>
        <Radio {...args} />
      </Box>
    )}
  </Story>
</Canvas>
