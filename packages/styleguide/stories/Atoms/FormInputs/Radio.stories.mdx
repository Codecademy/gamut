import { Radio } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={Radio}
  parameters={{
    subtitle:
      'A `<input type="radio" />` element to be used with a `<RadioGroup />` component',
    source: 'gamut',
    status: 'current',
  }}
  args={{
    htmlFor: 'example-radio',
    label: 'Option 1',
    name: 'example-radio',
  }}
/>

<Canvas>
  <Story name="Radio">{(args) => <Radio {...args} />}</Story>
</Canvas>

<PropsTable story="Radio" />

### Input States

<Canvas>
  <Story
    name="Disabled"
    args={{
      htmlFor: 'example-disabled',
      name: 'example-disabled',
      label: 'Disabled',
      disabled: true,
    }}
  >
    {(args) => <Radio {...args} />}
  </Story>
  <Story
    name="Disabled"
    args={{
      htmlFor: 'example-disabled-checked',
      name: 'example-disabled-checked',
      label: 'Disabled and Checked',
      disabled: true,
      checked: true,
    }}
  >
    {(args) => <Radio {...args} />}
  </Story>
  <Story
    name="Error"
    args={{
      htmlFor: 'example-error',
      label: 'Error',
      name: 'example-error',
      error: true,
    }}
  >
    {(args) => <Radio {...args} />}
  </Story>
</Canvas>

### With tooltip

A radio item may include a [`ToolTip`](/?path=/docs/atoms-tooltip--tool-tip). The position of the tooltip is set to the bottom-right by default.

<Canvas>
  <Story
    name="Tooltip"
    args={{
      htmlFor: 'example-tooltip',
      label: 'Option with tooltip',
      name: 'example-tooltip',
      tooltip: {
        id: 'checkbox-tooltip',
        children:
          'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.',
      },
    }}
  >
    {(args) => <Radio {...args} />}
  </Story>
</Canvas>
