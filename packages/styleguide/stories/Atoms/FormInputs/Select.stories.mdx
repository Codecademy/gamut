import { Box, FlexBox, Select, SelectDropdown } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { PropsTable } from '~styleguide/blocks';

export const fruitOptions = [
  'Apple',
  'Banana',
  'Cherry',
  'Dragonfruit',
  'Eggplant',
];

<Meta
  title={title}
  component={Select}
  subcomponent={SelectDropdown}
  parameters={{
    subtitle: 'A `<select />` element for picking from exclusive options',
    source: 'gamut',
    status: 'current',
  }}
  args={{
    htmlFor: 'example-select',
    name: 'example-select',
    options: fruitOptions,
  }}
  argTypes={{
    value: {
      control: {
        type: 'select',
        options: fruitOptions,
      },
    },
  }}
/>

<Canvas>
  <Story name="Select">{(args) => <Select value="Apple" {...args} />}</Story>
</Canvas>

<PropsTable story="Select" />

### Select States

<Canvas>
  <Story
    name="Disabled"
    args={{
      options: ['Disabled'],
      defaultValue: 'Disabled',
      disabled: true,
    }}
  >
    {(args) => <Select {...args} />}
  </Story>
  <Story
    name="Error"
    args={{ options: ['Error'], defaultValue: 'Error', error: true }}
  >
    {(args) => <Select {...args} />}
  </Story>
</Canvas>

### Select Size Variant

<Canvas>
  <Story
    name="Base"
    args={{
      options: [
        'Base',
        'Regular Size',
        'Normal',
        'lorem lorem lorem ipsum ipsum ipsum lorem',
      ],
      defaultValue: 'Regular Size',
    }}
  >
    {(args) => <Select {...args} />}
  </Story>
  <Story
    name="Small"
    args={{
      options: ['Small', 'Quite little'],
      defaultValue: 'Small',
      sizeVariant: 'small',
    }}
  >
    {(args) => <Select {...args} />}
  </Story>
</Canvas>

### SelectDropdown

<Canvas>
  <Story
    name="BaseDropdown"
    args={{
      options: ['Base', 'Regular Size', '"Normal'],
      placeholder: 'all about that',
    }}
  >
    {(args) => (
      <FlexBox width="10rem" height="25rem">
        <SelectDropdown {...args} />
      </FlexBox>
    )}
  </Story>
  <Story
    name="ErrorDropdown"
    args={{
      options: ['Small', 'Quite little'],
      defaultValue: 'Quite little',
      error: true,
    }}
  >
    {(args) => (
      <FlexBox
        width="10rem"
        height="25rem"
        backgroundColor="palePink"
        padding="1rem"
      >
        <SelectDropdown {...args} />
      </FlexBox>
    )}
  </Story>
  <Story
    name="DisabledDropdown"
    args={{
      options: ['Disabled'],
      disabled: true,
    }}
  >
    {(args) => (
      <FlexBox
        width="10rem"
        height="25rem"
        backgroundColor="paleGreen"
        padding="1rem"
      >
        <SelectDropdown {...args} />
      </FlexBox>
    )}
  </Story>
</Canvas>
