import { Box, Select, SelectDropdown } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { PropsTable } from '~styleguide/blocks';

export const fruitOptions = [
  'Apple',
  'Banana',
  'Cherry',
  'Dragonfruit',
  'Eggplant',
];

<Meta
  title={title}
  component={Select}
  subcomponent={SelectDropdown}
  parameters={{
    subtitle: 'A `<select />` element for picking from exclusive options',
    source: 'gamut',
    status: 'current',
  }}
  args={{
    htmlFor: 'example-select',
    name: 'example-select',
    options: fruitOptions,
  }}
  argTypes={{
    value: {
      control: {
        type: 'select',
        options: fruitOptions,
      },
    },
  }}
/>

<Canvas>
  <Story name="Select">{(args) => <Select value="Apple" {...args} />}</Story>
</Canvas>

<PropsTable story="Select" />

### Select States

<Canvas>
  <Story
    name="Disabled"
    args={{
      options: ['Disabled'],
      defaultValue: 'Disabled',
      disabled: true,
    }}
  >
    {(args) => <Select {...args} />}
  </Story>
  <Story
    name="Error"
    args={{ options: ['Error'], defaultValue: 'Error', error: true }}
  >
    {(args) => <Select {...args} />}
  </Story>
</Canvas>

### Select Size Variant

<Canvas>
  <Story
    name="Base"
    args={{
      options: ['Base', 'Regular Size', '"Normal'],
      defaultValue: 'Base',
    }}
  >
    {(args) => <Select {...args} />}
  </Story>
  <Story
    name="Small"
    args={{
      options: ['Small', 'Quite little'],
      defaultValue: 'Small',
      sizeVariant: 'small',
    }}
  >
    {(args) => <Select {...args} />}
  </Story>
</Canvas>

### SelectDropdown

<Canvas>
  <Story
    name="BaseDropdown"
    args={{
      options: ['Base', 'Regular Size', '"Normal'],
      placeholder: 'all about that',
    }}
  >
    {(args) => (
      <Box width="10rem">
        <SelectDropdown {...args} />
      </Box>
    )}
  </Story>
  <Story
    name="ErrorDropdown"
    args={{
      options: ['Small', 'Quite little'],
      defaultValue: 'Small',
      error: true,
    }}
  >
    {(args) => <SelectDropdown {...args} />}
  </Story>
  <Story
    name="DisabledDropdown"
    args={{
      options: ['Disabled'],
      defaultValue: 'Disabled',
      disabled: true,
    }}
  >
    {(args) => <SelectDropdown {...args} />}
  </Story>
</Canvas>
