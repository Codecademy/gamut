import { Checkbox } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={Checkbox}
  parameters={{
    subtitle:
      'A `<input type="checkbox" />` element for handling boolean values',
    source: 'gamut',
    status: 'stable',
  }}
  args={{
    htmlFor: 'example-checkbox',
    label: 'A Checkbox Label',
    name: 'example-checkbox',
  }}
/>

<Canvas>
  <Story name="Checkbox">{(args) => <Checkbox {...args} />}</Story>
</Canvas>

<PropsTable story="Checkbox" />

### Input States

<Canvas>
  <Story
    name="Checked"
    args={{
      htmlFor: 'example-checked',
      label: 'Checked',
      name: 'example-checked',
      checked: true,
    }}
  >
    {(args) => <Checkbox {...args} />}
  </Story>
  <Story
    name="Disabled"
    args={{
      htmlFor: 'example-disabled',
      label: 'Disabled',
      name: 'example-disabled',
      disabled: true,
    }}
  >
    {(args) => <Checkbox {...args} />}
  </Story>
  <Story
    name="Checked + Disabled"
    args={{
      htmlFor: 'example-checked-disabled',
      label: 'Checked + Disabled',
      name: 'example-checked-disabled',
      disabled: true,
      checked: true,
    }}
  >
    {(args) => <Checkbox {...args} />}
  </Story>
  <Story
    name="Multiline"
    args={{
      htmlFor: 'example-multiline',
      label: 'Multiline',
      name: 'example-multiline',
      label:
        'I am a longer label and will displayed at a smaller size in order to conserve valuable space',
      multiline: true,
    }}
  >
    {(args) => <Checkbox {...args} />}
  </Story>
  {/* <Story
    name="Error"
    args={{
      htmlFor: 'example-error',
      label: 'Error',
      name: 'example-error',
      error: true,
    }}
  >
    {(args) => <Checkbox {...args} />}
  </Story> */}
</Canvas>
