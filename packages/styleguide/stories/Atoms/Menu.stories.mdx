import { GridBox, Menu, MenuItem, MenuSeparator } from '@codecademy/gamut';
import { ColorMode } from '@codecademy/gamut-styles';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={Menu}
  subcomponents={{ MenuItem }}
  parameters={{
    subtitle: 'Menu Elements',
    source: 'gamut',
    status: 'current',
    figmaId: '',
  }}
  args={{}}
/>

## Variants

<Story name="Variants">
  <GridBox gridAutoFlow="column" gap={16}>
    <Menu variant="select">
      <MenuItem href="hello world">Link</MenuItem>
      <MenuItem active>Item</MenuItem>
      <MenuSeparator />
      <MenuItem>Item</MenuItem>
      <MenuItem>Cool beans!</MenuItem>
    </Menu>
    <Menu variant="action">
      <MenuItem href="hello world">Link</MenuItem>
      <MenuItem active>Item</MenuItem>
      <MenuSeparator />
      <MenuItem>Item</MenuItem>
      <MenuItem>Cool beans!</MenuItem>
    </Menu>
    <Menu variant="navigation">
      <MenuItem href="hello world">Link</MenuItem>
      <MenuItem active>Item</MenuItem>
      <MenuSeparator />
      <MenuItem>Item</MenuItem>
      <MenuItem>Cool beans!</MenuItem>
    </Menu>
  </GridBox>
</Story>

## Spacing

<Story name="Spacing">
  <GridBox gridAutoFlow="column" gap={16}>
    <Menu spacing="normal">
      <MenuItem href="hello world">Link</MenuItem>
      <MenuItem active>Item</MenuItem>
      <MenuSeparator />
      <MenuItem>Item</MenuItem>
      <MenuItem>Cool beans!</MenuItem>
    </Menu>
    <Menu spacing="condensed">
      <MenuItem href="hello world">Link</MenuItem>
      <MenuItem active>Item</MenuItem>
      <MenuSeparator />
      <MenuItem>Item</MenuItem>
      <MenuItem>Cool beans!</MenuItem>
    </Menu>
  </GridBox>
</Story>

<Canvas>
  <Story name="Menu">
    {(args) => (
      <GridBox gridTemplateColumns="repeat(3, max-content)">
        {['light', 'dark'].map((mode) => {
          return ['normal', 'condensed'].map((spacing) => {
            return ['action', 'select', 'navigation'].map((variant) => {
              return (
                <ColorMode mode={mode} p={16}>
                  <Menu spacing={spacing} variant={variant}>
                    <MenuItem href="hello world">Link</MenuItem>
                    <MenuItem active>Item</MenuItem>
                    <MenuSeparator />
                    <MenuItem>Item</MenuItem>
                    <MenuItem>Cool beans!</MenuItem>
                  </Menu>
                </ColorMode>
              );
            });
          });
        })}
      </GridBox>
    )}
  </Story>
</Canvas>

<PropsTable story="Menu" />
