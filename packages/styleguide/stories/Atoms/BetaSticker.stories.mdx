import { BetaSticker, Text } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={BetaSticker}
  parameters={{
    subtitle: `This Beta Sticker can be used for designating beta status in new features.`,
    source: 'gamut',
    status: 'current',
  }}
  args={{ backgroundColor: 'white' }}
/>

<Canvas withSource="open">
  <Story name="Default">{(args) => <BetaSticker {...args} />}</Story>
</Canvas>

<PropsTable story="Default" />

<Canvas>
  <Story name="Color">
    {(args) => {
      return (
        <Text color="hyper">
          <BetaSticker {...args} />
        </Text>
      );
    }}
  </Story>
</Canvas>
