import { CardShell, Text } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={CardShell}
  parameters={{
    subtitle: `A shadowed shell`,
    status: 'deprecated',
    source: 'gamut',
  }}
/>

<Canvas>
  <Story name="Card" args={{ hoverShadow: false }}>
    {(args) => (
      <CardShell>
        <Text as="h3">Card Body</Text>
        <Text>This is some body text</Text>
        <Text>Blah blah blurgha blurgha</Text>
        <Text>Footer Text</Text>
      </CardShell>
    )}
  </Story>
</Canvas>

<PropsTable story="Card" />
