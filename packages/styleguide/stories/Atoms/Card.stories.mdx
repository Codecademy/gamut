import { Anchor, Card, Column, LayoutGrid } from '@codecademy/gamut/src';
import { Background } from '@codecademy/gamut-styles/src';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { LinkTo } from '~styleguide/blocks';

<Meta
  title={title}
  component={Card}
  args={{
    children: 'Hello world I am a Card',
    padding: 16,
  }}
  parameters={{
    subtitle:
      'A card with multiple background combinations for presenting different types of information',
    status: 'updating',
    source: 'gamut',
  }}
/>

<Canvas>
  <Story name="Card">{(args) => <Card {...args} />}</Story>
</Canvas>

<PropsTable story="Card" />

## Variants

We provide 5 style variants for Card.

<Canvas>
  <Story name="White" args={{ variant: 'white' }}>
    {(args) => <Card {...args} />}
  </Story>
  <Story name="Yellow" args={{ variant: 'yellow' }}>
    {(args) => <Card {...args} />}
  </Story>
  <Story name="Navy" args={{ variant: 'navy' }}>
    {(args) => <Card {...args} />}
  </Story>
  <Story name="Hyper" args={{ variant: 'hyper' }}>
    {(args) => <Card {...args} />}
  </Story>
  <Story name="Beige" args={{ variant: 'beige' }}>
    {(args) => <Card {...args} />}
  </Story>
</Canvas>

## Shadow variants

We provide 3 style variants for shadow.

<Canvas>
  <Story name="Small" args={{ shadow: 'small' }}>
    {(args) => <Card {...args} />}
  </Story>
  <Story name="Medium" args={{ shadow: 'medium' }}>
    {(args) => <Card {...args} />}
  </Story>
  <Story name="Outline" args={{ shadow: 'outline' }}>
    {(args) => <Card {...args} />}
  </Story>
</Canvas>

### Shadow variant outline with color

The outline color will inherit the color of the card.

<Canvas>
  <Story
    name="Outline on Beige card"
    args={{ shadow: 'outline', variant: 'beige' }}
  >
    {(args) => <Card {...args} />}
  </Story>
</Canvas>

# ColorMode as default

Card elements respond to the current <LinkTo id="foundations-color-mode--page">ColorMode</LinkTo> they are used in. Note that their background color will match the background of their context and secondary colors are used for their hover effect. As these are not the typical inverse effects we utilize in color mode, please check with your design team before use.

<Story name="dynamic">
  {() => (
    <LayoutGrid>
      <Column size={6}>
        <Background
          p={32}
          bg="navy"
          borderColor="black"
          border={1}
          display="grid"
          gridTemplateColumns="repeat(3, 1fr)"
        >
          <Card height="100%" shadow="small">
            <Anchor variant={false}>Click Me!</Anchor>
          </Card>
        </Background>
      </Column>
      <Column size={6}>
        <Background
          p={32}
          bg="white"
          borderColor="black"
          border={1}
          display="grid"
          gridTemplateColumns="repeat(3, 1fr)"
        >
          <Card height="100%" shadow="small">
            <Anchor variant={false}>Click Me!</Anchor>
          </Card>
        </Background>
      </Column>
    </LayoutGrid>
  )}
</Story>

## As links

<Canvas>
  <Story name="Link" args={{ shadow: 'medium' }}>
    {(args) => (
      <Anchor variant="interface">
        <Card {...args} />
      </Anchor>
    )}
  </Story>
</Canvas>
