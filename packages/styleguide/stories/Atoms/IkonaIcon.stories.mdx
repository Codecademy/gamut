import { Meta, Props, Preview, Story } from '@storybook/addon-docs/dist/blocks';
import { StoryStatus } from '../Templating/Blocks';
import { LayoutGrid, Column, IkonaIcon } from '@codecademy/gamut/src';
import iconStyles from '@codecademy/gamut/src/IkonaIcon/styles/index.module.scss';
import { withKnobs } from '@storybook/addon-knobs';

<Meta
  title="Core|Atoms/IkonaIcon (Deprecated)"
  component={IkonaIcon}
  decorators={[withKnobs]}
/>

# IkonaIcon

<StoryStatus status="deprecated" />

Very old legacy icons as defined in the `@codecademy/gamut` package&apos;s `IkonaIcon` export.

Please use non-deprecated `Icon`s from the `@codecademy/gamut-icons` package instead.

<Preview>
  <Story name="Ikona Icons">
    <LayoutGrid rowGap="md" columnGap="md">
      {Object.keys(iconStyles)
        .filter((cn) => cn.match('ikona-'))
        .map((cn) => cn.replace('ikona-', ''))
        .map((iconName) => (
          <Column size={2}>
            <IkonaIcon key={iconName} name={iconName} size={2} />
            <br />
            <span>{iconName}</span>
          </Column>
        ))}
    </LayoutGrid>
  </Story>
</Preview>

<Props of={IkonaIcon} />
