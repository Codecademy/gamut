import { Badge } from '@codecademy/gamut/src';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { VisualTheme } from '@codecademy/gamut/src';
import { withKnobs, select } from '@storybook/addon-knobs';

<Meta title="Core|Atoms/Badgers" decorators={[withKnobs]} component={Badge} />

# Badges

Small blue indicator of some unusual text

<Preview>
  <Story name="Badge">
    {() => (
      <p>
        Use it inline with the text to be decorated. <Badge>New</Badge>
      </p>
    )}
  </Story>
</Preview>

<Props of={Badge} />
