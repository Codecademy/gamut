import { Spinner } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Props, Story } from '@storybook/addon-docs/dist/blocks';
import { text } from '@storybook/addon-knobs';

<Meta
  title={title}
  component={Spinner}
  parameters={{
    subtitle: `Perpetually spinning circle, such as for a loading indicator. Similar to a
\`ProgressBar\`.`,
    source: 'gamut',
  }}
/>

<Canvas>
  <Story name="Spinner">
    {(args) => <Spinner size={text('size', '5em')} />}
  </Story>
</Canvas>

<Props of={Spinner} />
