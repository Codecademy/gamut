import { Spinner } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={Spinner}
  parameters={{
    subtitle: `Perpetually spinning circle, such as for a loading indicator. Similar to a
\`ProgressBar\`.`,
    source: 'gamut',
  }}
  args={{ size: 60 }}
/>

<Canvas>
  <Story name="Spinner">{(args) => <Spinner {...args} />}</Story>
</Canvas>

<PropsTable story="Spinner" />
