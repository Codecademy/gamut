import { Meta, Props, Preview, Story } from '@storybook/addon-docs/dist/blocks';
import { StoryStatus } from '../Templating/Blocks';
import { text, withKnobs } from '@storybook/addon-knobs';
import { Spinner } from '@codecademy/gamut/src';

<Meta title="Core|Atoms/Spinner" component={Spinner} decorators={[withKnobs]} />

# Spinner

<StoryStatus status="ready" />

Perpetually spinning circle, such as for a loading indicator. Similar to a
`ProgressBar`.

<Preview>
  <Story name="Spinner">
    <Spinner size={text('size', '5em')} />
  </Story>
</Preview>

<Props of={Spinner} />
