import { FormError } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={FormError}
  parameters={{
    subtitle: 'A stylized wrapper for validation errors',
    source: 'gamut',
    status: 'stable',
  }}
  args={{ children: 'Uh oh something went wrong!' }}
/>

<Canvas>
  <Story name="FormError">{(args) => <FormError {...args} />}</Story>
</Canvas>

<PropsTable story="FormError" />
