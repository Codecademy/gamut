import { FormRequiredText } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta
  title={title}
  component={FormRequiredText}
  parameters={{
    subtitle: 'A Text element that renders our required field explanation, should be included in all forms with required fields.',
    source: 'gamut',
    status: 'current',
  }}
/>

`FormRequiredText` should be included in all forms with required fields. Accepts all props of [our existing `Text`](/?path=/docs/typography-text--element-variantst) component.

<Canvas>
  <Story name="R">
    {(args) => <FormRequiredText {...args} />}
  </Story>
</Canvas>

<PropsTable story="FormRequiredText" />
