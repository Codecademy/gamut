import title from '@codecademy/macros/lib/title.macro';
import { Meta } from '@storybook/addon-docs/blocks';
import LinkTo from '@storybook/addon-links/react';

import { TableOfContents } from '~styleguide/blocks';

<Meta
  title={title}
  parameters={{
    subtitle: `Ancillary descriptions for input atoms tied 1:1 with values to
be submitted in a form.`,
  }}
/>

You should generally never use these directly as a Gamut consumer;
instead, use <LinkTo kind='Organisms/GridForm'>GridForm</LinkTo> to auto-generate these for you.

<TableOfContents />
