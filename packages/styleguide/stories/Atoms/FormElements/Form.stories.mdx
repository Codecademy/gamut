import { Form } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={Form}
  parameters={{
    subtitle:
      'A customizable `<form />` element for wrapping other form elements',
    source: 'gamut',
    status: 'current',
  }}
  args={{}}
/>

<Canvas>
  <Story name="Form">{(args) => <Form {...args} />}</Story>
</Canvas>

<PropsTable story="Form" />
