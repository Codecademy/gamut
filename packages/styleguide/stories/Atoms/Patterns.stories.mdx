import * as patterns from '@codecademy/gamut-patterns';
import { colors } from '@codecademy/gamut-styles';
import title from '@codecademy/macros/lib/title.macro';
import {
  IconGallery,
  IconItem,
  Meta,
  Preview,
  Story,
} from '@storybook/addon-docs/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  parameters={{
    subtitle: `We're developing a set of fun patterns.`,
    source: 'gamut-patterns',
    status: 'updating',
    figmaId:
      'j0vUEMpEJyA5lf5J0uPgJC/,Pattern-Fills-12.14.20?node-id=407%3A17502',
  }}
  component={patterns.DotLoose}
/>

## General Information

These are intended to be able to take in a color and any system layout, positioning, and spacing props.

## Sandbox

<Preview>
  <Story name="Pattern">
    <patterns.DotLoose color={colors.hyper} height={200} />
  </Story>
</Preview>

<PropsTable of={patterns.DotLoose} />

## Usage

<IconGallery>
  {Object.entries(patterns).map(([patternName, Pattern]) => (
    <IconItem key={patternName} name={patternName}>
      <Pattern width="128px" />
    </IconItem>
  ))}
</IconGallery>
