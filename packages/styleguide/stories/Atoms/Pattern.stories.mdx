import { Pattern } from '@codecademy/gamut/src';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={Pattern}
  parameters={{
    source: 'gamut',
    status: 'updating',
    subtitle: 'Repeatedable pattern backgrounds for multiple uses.',
  }}
  args={{ height: 200, width: 200, name: 'diagonalStripesLoose' }}
/>

<Canvas>
  <Story name="Pattern" args={{ width: '100%', height: 200 }}>
    {(args) => <Pattern {...args} />}
  </Story>
</Canvas>

<PropsTable story="Pattern" />

<Canvas withSource="open">
  <Story name="Pattern Loose">{(args) => <Pattern {...args} />}</Story>
  <Story name="Pattern Regular" args={{ name: 'diagonalStripesRegular' }}>
    {(args) => <Pattern {...args} />}
  </Story>
  <Story name="Pattern Dense" args={{ name: 'diagonalStripesDense' }}>
    {(args) => <Pattern {...args} />}
  </Story>
  <Story name="Pattern: Checker Loose" args={{ name: 'checkerLoose' }}>
    {(args) => <Pattern {...args} />}
  </Story>
  <Story name="Pattern: Checker Regular" args={{ name: 'checkerRegular' }}>
    {(args) => <Pattern {...args} />}
  </Story>
  <Story name="Pattern: Checker Dense" args={{ name: 'checkerDense' }}>
    {(args) => <Pattern {...args} />}
  </Story>
  <Story name="Pattern: Dot Loose" args={{ name: 'dotLoose' }}>
    {(args) => <Pattern {...args} />}
  </Story>
</Canvas>
