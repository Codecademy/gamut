import { TextButton } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={TextButton}
  parameters={{
    subtitle: 'A button that only renders an icon',
    source: 'gamut',
    status: 'stable',
    figmaId: 'N6TfoPI5OA7GanX6Zl8nWj',
  }}
  args={{
    children: 'Click Me',
    disabled: false,
    href: '',
    mode: 'light',
    size: 'normal',
  }}
/>

<Canvas>
  <Story name="TextButton">{(args) => <TextButton {...args} />}</Story>
</Canvas>

<PropsTable story="TextButton" />

## Modes

<Canvas>
  <Story name="light" args={{ mode: 'light', children: 'light' }}>
    {(args) => <TextButton {...args} />}
  </Story>
  <Story
    name="light-disabled"
    args={{ disabled: true, children: 'light-disabled' }}
  >
    {(args) => <TextButton {...args} />}
  </Story>
</Canvas>

<Canvas>
  <Story name="dark" args={{ mode: 'dark', children: 'dark' }}>
    {(args) => <TextButton {...args} />}
  </Story>
  <Story
    name="dark-disabled"
    args={{ mode: 'dark', disabled: true, children: 'dark-disabled' }}
  >
    {(args) => <TextButton {...args} />}
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story name="normal" args={{ size: 'normal', children: 'normal' }}>
    {(args) => <TextButton {...args} />}
  </Story>
  <Story name="small" args={{ size: 'small', children: 'small' }}>
    {(args) => <TextButton {...args} />}
  </Story>
</Canvas>
