import { StrokeButton } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { Canvas, Meta, Story } from '@storybook/addon-docs/dist/blocks';

import { PropsTable } from '~styleguide/blocks';

<Meta
  title={title}
  component={StrokeButton}
  parameters={{
    subtitle: 'A button that only renders an icon',
    source: 'gamut',
    status: 'stable',
    figmaId: 'N6TfoPI5OA7GanX6Zl8nWj',
  }}
  args={{
    children: 'Click Me',
    disabled: false,
    href: '',
    mode: 'light',
    size: 'normal',
  }}
/>

<Canvas>
  <Story name="StrokeButton">{(args) => <StrokeButton {...args} />}</Story>
</Canvas>

<PropsTable story="StrokeButton" />

## Modes

<Canvas>
  <Story name="Light" args={{ mode: 'light', children: 'light' }}>
    {(args) => <StrokeButton {...args} />}
  </Story>
  <Story name="Dark" args={{ mode: 'dark', children: 'dark' }}>
    {(args) => <StrokeButton {...args} />}
  </Story>
</Canvas>

## States

<Canvas>
  <Story name="Disabled" args={{ disabled: true, children: 'disabled' }}>
    {(args) => <StrokeButton {...args} />}
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story name="Normal" args={{ size: 'normal', children: 'normal' }}>
    {(args) => <StrokeButton {...args} />}
  </Story>
  <Story name="Small" args={{ size: 'small', children: 'small' }}>
    {(args) => <StrokeButton {...args} />}
  </Story>
</Canvas>
