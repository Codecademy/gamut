import { Meta } from '@storybook/blocks';

import { AboutHeader, TableOfContents } from '~styleguide/blocks';

import { parameters as DataListParameters } from './DataList/DataList.mdx';
import { parameters as DataTableParameters } from './DataTable/DataTable.mdx';
import { parameters as ListParameters } from './List/List.mdx';

export const parameters = {
  id: 'Organisms/Lists&Tables',
  title: 'Lists & Tables',
  subtitle:
    'Lists & Tables are both organisms that render repeated, interactive information.',
};

<Meta title="Organisms/Lists&Tables/About" />

<AboutHeader {...parameters} />

Both `List` and `Table` are commonly used to showcase objects, data, and information in powerful ways. While both Lists and Tables are similar and share the same underlying DataList structure, they both have different presentational templates and design best practices that will help you determine which component to use. Read on to figure out how to choose the best information design for your elements.

<TableOfContents
  links={[
    {
      id: `Organisms/${parameters.title}/${DataListParameters.title}`,
      subtitle: DataListParameters.subtitle,
      title: DataListParameters.title,
      status: DataListParameters.status,
    },
    {
      id: `Organisms/${parameters.title}/${DataTableParameters.title}`,
      subtitle: DataTableParameters.subtitle,
      title: DataTableParameters.title,
      status: DataTableParameters.status,
    },
    {
      id: `Organisms/${parameters.title}/${ListParameters.title}`,
      subtitle: ListParameters.subtitle,
      title: ListParameters.title,
      status: ListParameters.status,
    },
  ]}
/>
