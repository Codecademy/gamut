import { Meta } from '@storybook/blocks';

import { AboutHeader, TableOfContents } from '~styleguide/blocks';

import { parameters as connectedFormParameters } from './ConnectedForm/About.mdx';
import { parameters as gridFormParameters } from './GridForm/GridForm.mdx';
import { parameters as listsTablesParameters } from './Lists & Tables/About.mdx';
import { parameters as markdownParameters } from './Markdown/Markdown.mdx';

export const parameters = {
  id: 'Organisms',
  title: 'Organisms',
  subtitle:
    'Organisms are groups of molecules joined together to form a relatively complex, distinct section of an interface.',
};

<Meta title="Organisms/About" />

<AboutHeader {...parameters} />

> Molecules give us some building blocks to work with, and we can now combine them together to form organisms.
> Organisms are groups of molecules joined together to form a relatively complex, distinct section of an interface.
> [- Brad Frost, author of "Atomic Design"](https://bradfrost.com/blog/post/atomic-web-design/#organisms)

<TableOfContents
  links={[
    {
      id: `${parameters.title}/${connectedFormParameters.title}`,
      subtitle: connectedFormParameters.subtitle,
      title: connectedFormParameters.title,
      status: connectedFormParameters.status || 'current',
    },
    {
      id: `${parameters.title}/${gridFormParameters.title}`,
      subtitle: gridFormParameters.subtitle,
      title: gridFormParameters.title,
      status: gridFormParameters.status,
    },
    {
      id: `${parameters.title}/${listsTablesParameters.title}/About`,
      subtitle: listsTablesParameters.subtitle,
      title: listsTablesParameters.title,
      status: listsTablesParameters.status || 'current',
    },
    {
      id: `${parameters.title}/${markdownParameters.title}`,
      subtitle: markdownParameters.subtitle,
      title: markdownParameters.title,
      status: markdownParameters.status,
    },
  ]}
/>
