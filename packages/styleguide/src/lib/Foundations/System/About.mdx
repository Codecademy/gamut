import { Meta } from '@storybook/blocks';

import { AboutHeader, TableOfContents } from '~styleguide/blocks';

import { parameters as composeParameters } from './Compose.mdx';
import { parameters as propsParameters } from './Props.mdx';
import { parameters as responsivePropertiesParameters } from './ResponsiveProperties/ResponsiveProperties.mdx';
import { parameters as variantsParameters } from './Variants.mdx';

export const parameters = {
  title: 'System',
  subtitle: `System properties and style functions`,
  status: 'current',
};

<Meta title="Foundations/System/About" />

<AboutHeader {...parameters} />

Many of our components are written in our CSS in JS library, [Emotion](https://emotion.sh/). we've created a set of style utilities that we call **System Props** to improve reusability, predictability, and standardization across our design system.

These functions are built with [`@codecademy/variance`](https://github.com/Codecademy/gamut/tree/main/packages/variance): a library inspired by [`styled-system`](https://styled-system.com/) but with a focus on typesafety.

<TableOfContents
  links={[
    {
      id: `Foundations/${parameters.title}/${composeParameters.title}`,
      subtitle: composeParameters.subtitle,
      title: composeParameters.title,
    },
    {
      id: `Foundations/${parameters.title}/${propsParameters.title}`,
      subtitle: propsParameters.subtitle,
      title: propsParameters.title,
    },
    {
      id: `Foundations/${parameters.title}/${responsivePropertiesParameters.title}`,
      subtitle: responsivePropertiesParameters.subtitle,
      title: responsivePropertiesParameters.title,
    },
    {
      id: `Foundations/${parameters.title}/${variantsParameters.title}`,
      subtitle: variantsParameters.subtitle,
      title: variantsParameters.title,
    },
  ]}
/>
