import { Meta } from '@storybook/blocks';

import { AboutHeader, TableOfContents } from '~styleguide/blocks';

import { parameters as delayedRenderWrapperParameters } from './DelayedRenderWrapper/DelayedRenderWrapper.mdx';
import { parameters as focusTrapParameters } from './FocusTrap/FocusTrap.mdx';

export const parameters = {
  id: 'UtilityComponents',
  title: 'UtilityComponents',
  subtitle:
    'A collection of utility components that provide functionality to other components.',
  status: 'current',
};

<Meta title="Atoms/UtilityComponents/About" />

<AboutHeader {...parameters} />

<TableOfContents
  links={[
    {
      id: `Atoms/${parameters.title}/${delayedRenderWrapperParameters.title}`,
      subtitle: delayedRenderWrapperParameters.subtitle,
      title: delayedRenderWrapperParameters.title,
      status: delayedRenderWrapperParameters.status,
    },
    {
      id: `Atoms/${parameters.title}/${focusTrapParameters.title}`,
      subtitle: focusTrapParameters.subtitle,
      title: focusTrapParameters.title,
      status: focusTrapParameters.status,
    },
  ]}
/>
