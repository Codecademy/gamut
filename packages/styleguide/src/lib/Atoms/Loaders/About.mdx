import { Meta } from '@storybook/blocks';

import { AboutHeader, TableOfContents } from '~styleguide/blocks';

import { parameters as shimmerParameters } from './Shimmer/Shimmer.mdx';
import { parameters as spinnerParameters } from './Spinner/Spinner.mdx';

export const parameters = {
  id: 'Loaders',
  title: 'Loaders',
  subtitle: 'Animations that indicate information is currently loading.',
};

<Meta title="Atoms/Loaders/About" />

<AboutHeader {...parameters} />

<TableOfContents
  links={[
    {
      id: `Atoms/${parameters.title}/${shimmerParameters.title}`,
      subtitle: shimmerParameters.subtitle,
      title: shimmerParameters.title,
      status: shimmerParameters.status,
    },
    {
      id: `Atoms/${parameters.title}/${spinnerParameters.title}`,
      subtitle: spinnerParameters.subtitle,
      title: spinnerParameters.title,
      status: spinnerParameters.status,
    },
  ]}
/>
