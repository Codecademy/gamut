import { Canvas, Controls, Meta } from '@storybook/blocks';

import { ComponentHeader, LinkTo } from '~styleguide/blocks';

import * as SelectStories from './Select.stories';

export const parameters = {
  title: 'Select',
  subtitle: 'A `<select />` element for picking from exclusive options',
  design: {
    type: 'figma',
    url: 'https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=1189%3A0',
  },
  status: 'current',
  source: {
    repo: 'gamut',
    githubLink:
      'https://github.com/Codecademy/gamut/blob/main/packages/gamut/src/Form/inputs/Select.tsx',
  },
};

<Meta of={SelectStories} />

<ComponentHeader {...parameters} />

## Usage

Use Select to handle single selection within a group of options.

## Size variants

### Base

<Canvas of={SelectStories.Base} />

### Small

<Canvas of={SelectStories.Small} />

## Select states

### Disabled

<Canvas of={SelectStories.Disabled} />

### Error

<Canvas of={SelectStories.Error} />

## FormGroup + Select

`Select` formats nicely in FormGroups and reflects error states. Make sure you provide an htmlFor to the `FormGroup` and a `name` to the `Select` for maximum accessibility. See <LinkTo id="Atoms/FormElements/FormGroup">FormGroup</LinkTo> story for the customizations available.

<Canvas of={SelectStories.FormGroupBase} />
<Canvas of={SelectStories.FormGroupSmall} />

## Playground

<Canvas sourceState="shown" of={SelectStories.Default} />

<Controls />
