import { Canvas, Controls, Meta } from '@storybook/blocks';

import { ComponentHeader } from '~styleguide/blocks';

import * as RotationStories from './Rotation.stories';

export const parameters = {
  title: 'Rotation',
  subtitle: 'A container that can rotate its children.',
  status: 'current',
  source: {
    repo: 'gamut',
    githubLink:
      'https://github.com/Codecademy/gamut/blob/main/packages/gamut/src/Animation/Rotation.tsx',
  },
};

<Meta of={RotationStories} />

<ComponentHeader {...parameters} />

## Usage

The Rotation component is a container that can rotate its children and specify the degree of the rotation.

One common usage is to set the rotation of an arrow icon to indicate that a component can expand and then rotate back to collapse.

## Playground

<Canvas sourceState="shown" of={RotationStories.Default} />

<Controls />
