import { Meta } from '@storybook/blocks';

import { AboutHeader, TableOfContents } from '~styleguide/blocks';

import { parameters as accordionButtonDeprecatedParameters } from './AccordionButtonDeprecated/AccordionButtonDeprecated.mdx';
import { parameters as accordionDeprecatedParameters } from './AccordionDeprecated/AccordionDeprecated.mdx';
import { parameters as alertParameters } from './Alert/Alert.mdx';
import { parameters as breadcrumbsParameters } from './Breadcrumbs/Breadcrumbs.mdx';
import { parameters as coachmarkParameters } from './Coachmark/Coachmark.mdx';
import { parameters as disclosureParameters } from './Disclosure/Disclosure.mdx';
import { parameters as flyoutParameters } from './Flyout/Flyout.mdx';
import { parameters as menuParameters } from './Menu/Menu.mdx';
import { parameters as modalsParameters } from './Modals/About.mdx';
import { parameters as paginationParameters } from './Pagination/Pagination.mdx';
import { parameters as popoverParameters } from './Popover/Popover.mdx';
import { parameters as tabsParameters } from './Tabs/Tabs.mdx';
import { parameters as tipsParameters } from './Tips/About.mdx';
import { parameters as toastsParameters } from './Toasts/About.mdx';
import { parameters as videoParameters } from './Video/Video.mdx';

export const parameters = {
  id: 'Molecules',
  title: 'Molecules',
  subtitle:
    'Molecules are groups of atoms together. These consist of a few atoms combined for a small, self-contained design.',
};

<Meta title="Molecules/About" />

<AboutHeader {...parameters} />

> Things start getting more interesting and tangible when we start combining atoms together.
> Molecules are groups of atoms bonded together and are the smallest fundamental units of a compound.
> These molecules take on their own properties and serve as the backbone of our design systems.
>
> [- Brad Frost, author of "Atomic Design"](https://bradfrost.com/blog/post/atomic-web-design/#molecules)

<TableOfContents
  links={[
    {
      id: `${parameters.title}/${accordionButtonDeprecatedParameters.title}`,
      subtitle: accordionButtonDeprecatedParameters.subtitle,
      title: accordionButtonDeprecatedParameters.title,
      status: accordionButtonDeprecatedParameters.status,
    },
    {
      id: `${parameters.title}/${accordionDeprecatedParameters.title}`,
      subtitle: accordionDeprecatedParameters.subtitle,
      title: accordionDeprecatedParameters.title,
      status: accordionDeprecatedParameters.status,
    },
    {
      id: `${parameters.title}/${alertParameters.title}`,
      subtitle: alertParameters.subtitle,
      title: alertParameters.title,
      status: alertParameters.status,
    },
    {
      id: `${parameters.title}/${breadcrumbsParameters.title}`,
      subtitle: breadcrumbsParameters.subtitle,
      title: breadcrumbsParameters.title,
      status: breadcrumbsParameters.status,
    },
    {
      id: `${parameters.title}/${coachmarkParameters.title}`,
      subtitle: coachmarkParameters.subtitle,
      title: coachmarkParameters.title,
      status: coachmarkParameters.status,
    },
    {
      id: `${parameters.title}/${disclosureParameters.title}`,
      subtitle: disclosureParameters.subtitle,
      title: disclosureParameters.title,
      status: disclosureParameters.status,
    },
    {
      id: `${parameters.title}/${flyoutParameters.title}`,
      subtitle: flyoutParameters.subtitle,
      title: flyoutParameters.title,
      status: flyoutParameters.status,
    },
    {
      id: `${parameters.title}/${menuParameters.title}`,
      subtitle: menuParameters.subtitle,
      title: menuParameters.title,
      status: menuParameters.status,
    },
    {
      id: `${parameters.title}/${modalsParameters.title}`,
      subtitle: modalsParameters.subtitle,
      title: modalsParameters.title,
      status: modalsParameters.status,
    },
    {
      id: `${parameters.title}/${paginationParameters.title}`,
      subtitle: paginationParameters.subtitle,
      title: paginationParameters.title,
      status: paginationParameters.status,
    },
    {
      id: `${parameters.title}/${popoverParameters.title}`,
      subtitle: popoverParameters.subtitle,
      title: popoverParameters.title,
      status: popoverParameters.status,
    },
    {
      id: `${parameters.title}/${tabsParameters.title}`,
      subtitle: tabsParameters.subtitle,
      title: tabsParameters.title,
      status: tabsParameters.status,
    },
    {
      id: `${parameters.title}/${tipsParameters.title}`,
      subtitle: tipsParameters.subtitle,
      title: tipsParameters.title,
      status: tipsParameters.status,
    },
    {
      id: `${parameters.title}/${toastsParameters.title}`,
      subtitle: toastsParameters.subtitle,
      title: toastsParameters.title,
      status: toastsParameters.status,
    },
    {
      id: `${parameters.title}/${videoParameters.title}`,
      subtitle: videoParameters.subtitle,
      title: videoParameters.title,
      status: videoParameters.status,
    },
  ]}
/>
