import { Canvas, Controls, Meta, Story } from '@storybook/blocks';

import { ComponentHeader } from '~styleguide/blocks';

import * as BreadcrumbStories from './Breadcrumbs.stories';

export const parameters = {
  subtitle: `Breadcrumb-style links for upwards navigation.`,
  source: 'gamut',
  status: 'current',
  design: {
    type: 'figma',
    url: 'https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=30186-40570&m=dev',
  },
};

<Meta of={BreadcrumbStories} />

<ComponentHeader {...parameters} />

## Usage

Use breadcrumbs to help users understand where they are in a site's hierarchy and to navigate back to previous pages.

### Best practices:
- It's recommended to use breadcrumbs on pages with a hierarchy of multiple levels.

## Variants
### Default

Breadcrumbs can be solely used for navigation.

<Canvas of={BreadcrumbStories.Default} />

### Payload

Breadcrumbs can also have payloads that can be used to pass data to an `onClick` event.

<Canvas of={BreadcrumbStories.Payload} />

## Playground

<Canvas sourceState="shown">
  <Story of={BreadcrumbStories.Default} />
</Canvas>

<Controls />
