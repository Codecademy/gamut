import { Meta } from '@storybook/addon-docs/blocks';

import {
  AboutHeader,
  addParentPath,
  TableOfContents,
} from '~styleguide/blocks';

import { parameters as codeConnectParameters } from './Code Connect.mdx';
import { parameters as figmaMcpParameters } from './Figma MCP.mdx';

export const parameters = {
  id: 'Meta/Figma Tooling',
  title: 'Figma Tooling',
  subtitle:
    'Figma Code Connect, Dev Mode MCP, and related Figma workflows for Codecademy + Skillsoft employees.',
  status: 'static',
};

<Meta title="Meta/Figma Tooling/About" />

<AboutHeader {...parameters} />

<TableOfContents
  links={addParentPath(parameters.id, [
    codeConnectParameters,
    figmaMcpParameters,
  ])}
/>
