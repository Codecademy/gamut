import { Meta } from '@storybook/blocks';

import { AboutHeader, LinkTo, TableOfContents } from '~styleguide/blocks';

import { parameters as AlertsParameters } from './Alerts.mdx';
import { parameters as AlternativeTextParameters } from './Alternative text.mdx';
import { parameters as ButtonsParameters } from './Buttons.mdx';
import { parameters as ConfirmationDialogsParameters } from './Confirmation dialogs.mdx';
import { parameters as ErrorMessagesParameters } from './Error messages.mdx';
import { parameters as NotificationsParameters } from './Notifications.mdx';
import { parameters as ToastsParameters } from './Toasts.mdx';
import { parameters as TooltipsParameters } from './Tooltips.mdx';

export const parameters = {
  id: 'Component guidelines',
  title: 'Component guidelines',
  subtitle:
    'Get up to speed on best practices for writing for user interfaces, including guidance for writing for specific components in Gamut.',
};

<Meta title="UX Writing/Component guidelines/About" />

<AboutHeader {...parameters} />

Writing on your own? Use the <LinkTo id="UX Writing/DIY UX writing in 8 steps">step-by-step guide</LinkTo> to ensure the copy is clear and useful.

<TableOfContents
  links={[
    {
      id: `UX Writing/${parameters.title}/${AlertsParameters.title}`,
      subtitle: AlertsParameters.subtitle,
      title: AlertsParameters.title,
    },
    {
      id: `UX Writing/${parameters.title}/${AlternativeTextParameters.title}`,
      title: AlternativeTextParameters.title,
      subtitle: AlternativeTextParameters.subtitle,
    },
    {
      id: `UX Writing/${parameters.title}/${ButtonsParameters.title}`,
      subtitle: ButtonsParameters.subtitle,
      title: ButtonsParameters.title,
    },
    {
      id: `UX Writing/${parameters.title}/${ConfirmationDialogsParameters.title}`,
      subtitle: ConfirmationDialogsParameters.subtitle,
      title: ConfirmationDialogsParameters.title,
    },
    {
      id: `UX Writing/${parameters.title}/${ErrorMessagesParameters.title}`,
      subtitle: ErrorMessagesParameters.subtitle,
      title: ErrorMessagesParameters.title,
    },
    {
      id: `UX Writing/${parameters.title}/${NotificationsParameters.title}`,
      subtitle: NotificationsParameters.subtitle,
      title: NotificationsParameters.title,
    },
    {
      id: `UX Writing/${parameters.title}/${ToastsParameters.title}`,
      subtitle: ToastsParameters.subtitle,
      title: ToastsParameters.title,
    },
    {
      id: `UX Writing/${parameters.title}/${TooltipsParameters.title}`,
      subtitle: TooltipsParameters.subtitle,
      title: TooltipsParameters.title,
    },
  ]}
/>
