import { Canvas, Controls, Meta } from '@storybook/blocks';

import { ComponentHeader, LinkTo } from '~styleguide/blocks';

import * as FlexBoxStories from './FlexBox.stories';

export const parameters = {
  title: 'FlexBox',
  subtitle: 'a flex container component with all system props available',
  status: 'current',
  source: {
    repo: 'gamut',
    githubLink:
      'https://github.com/Codecademy/gamut/blob/main/packages/gamut/src/Box/FlexBox.tsx',
  },
};

<Meta of={FlexBoxStories} />

<ComponentHeader {...parameters} />

For flex layouts we've added a specific flex <code>FlexBox</code> component that extends <LinkTo id="Layouts/Boxes/Box">Box</LinkTo> with extra props for common flex configurations.

## Usage guidelines

- Use this component if you need specific flex behaviors that can only be used by `flex` and `inline-flex` elements.
- Do not use this component if you want access to flex child props like `flex` or `alignSelf` as most components will have access to these props directly!

## States

`FlexBox` comes with a few helpful boolean props to make configuration easier (in addition to those on `Box`!), these exist as sorthands to make common configurations of props less verbose:

```tsx
// display="inline-flex"
<FlexBox inline />

// flexWrap="wrap"
<FlexBox wrap />

// alignItems="center" justifyContent="center"
<FlexBox center />

// flexDirection="row"
<FlexBox row />

// flexDirection="column"
<FlexBox column />
```

## Code playground

Edit attributes and see what’s possible to do with this component or get the exact output you want.

## Playground

<Canvas sourceState="shown" />

<Controls />
