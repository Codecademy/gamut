import { Meta } from '@storybook/blocks';

import { AboutHeader } from '~styleguide/blocks';

export const parameters = {
  id: 'Boxes',
  title: 'Boxes',
  subtitle: 'Flexible container component with all system props available.',
};

<Meta title="Layouts/Boxes/About" name="Layouts/Boxes/About" />

<AboutHeader {...parameters} />

Primitive box component that are meant to be a kitchen sink utility component for all basic layouts. These are meant to act as a configurable building block for almost any occasion. They are extremely flexible and have access to nearly all of our system props.

BOX: Box
FlexBox: Box but FlexBox
GridBox: Box but GridBox
TO-DO: Add links to boxes
