import { Meta } from '@storybook/blocks';

import { AboutHeader, TableOfContents } from '~styleguide/blocks';

export const parameters = {
  id: 'Boxes',
  title: 'Boxes',
  subtitle: 'Flexible container component with all system props available.',
};

<Meta title="Layouts/Boxes/About" name="Layouts/Boxes/About" />

<AboutHeader {...parameters} />

Primitive box component that are meant to be a kitchen sink utility component for all basic layouts. These are meant to act as a configurable building block for almost any occasion. They are extremely flexible and have access to nearly all of our system props.

<TableOfContents
  links={[
    {
      id: 'Layouts/Boxes/Box',
      status: 'current',
      subtitle:
        'Our primitive box component that is meant to be a kitchen sink utility component for all basic layouts',
      title: 'Box',
    },
    {
      id: 'Layouts/Boxes/FlexBox',
      status: 'current',
      subtitle:
        'A specific flex component that extends `Box` with extra props for common flex configurations.',
      title: 'FlexBox',
    },
    {
      id: 'Layouts/Boxes/GridBox',
      status: 'current',
      subtitle:
        'A specific grid component that extends `Box` with extra props for common grid configurations.',
      title: 'GridBox',
    },
  ]}
/>
