import { system, theme } from '@codecademy/gamut-styles';
import { StandardPropertiesHyphen } from 'csstype';

const radioWrapper = system.css({
  margin: '0.25rem 0',
  width: '100%',
  fontWeight: 'normal',
});
