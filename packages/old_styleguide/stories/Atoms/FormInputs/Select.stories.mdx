import { FormGroup, Select } from '@codecademy/gamut';
import title from '@codecademy/macros/lib/title.macro';
import { PropsTable } from '@codecademy/storybook-addon-variance';
import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

export const fruitOptions = [
  'Apple',
  'Banana',
  'Cherry',
  'Dragonfruit',
  'Eggplant',
];

export const fruitObjects = {
  month: 'Pro Monthly - $39.99 a month',
  annual: 'Pro Annual - $19.99 a month',
};

export const selectObject = {
  red: 'red',
  yellow: 'yellow',
  green: 'green',
};

<Meta
  title={title}
  component={Select}
  parameters={{
    subtitle: 'A `<select />` element for picking from exclusive options',
    source: 'gamut',
    status: 'current',
    design: {
      type: 'figma',
      url:
        'https://www.figma.com/file/ReGfRNillGABAj5SlITalN/%F0%9F%93%90-Gamut?node-id=1189%3A0',
    },
  }}
  args={{
    htmlFor: 'example-select',
    name: 'example-select',
    options: fruitOptions,
  }}
  argTypes={{
    value: {
      control: {
        type: 'select',
        options: fruitOptions,
      },
    },
  }}
/>

<Canvas>
  <Story name="Select">{(args) => <Select value="Apple" {...args} />}</Story>
</Canvas>

<PropsTable story="Select" />

### Select states

<Canvas>
  <Story
    name="Disabled"
    args={{
      options: ['Disabled'],
      defaultValue: 'Disabled',
      disabled: true,
    }}
  >
    {(args) => <Select {...args} />}
  </Story>
  <Story
    name="Error"
    args={{ options: ['Error'], defaultValue: 'Error', error: true }}
  >
    {() => <Select options={fruitObjects} />}
  </Story>
</Canvas>

### Select size variant

<Canvas>
  <Story
    name="Base"
    args={{
      options: ['Base', 'Regular Size', 'Normal'],
      defaultValue: 'Regular Size',
    }}
  >
    {(args) => <Select {...args} />}
  </Story>
  <Story
    name="Small"
    args={{
      options: ['Small', 'Quite little'],
      defaultValue: 'Small',
      sizeVariant: 'small',
      id: 'small-variant',
    }}
  >
    {(args) => <Select {...args} />}
  </Story>
</Canvas>

### FormGroup + Select

<Canvas>
  <Story
    name="FormGroupBase"
    args={{
      options: ['Error', 'oh no', ':('],
      defaultValue: 'oh no',
    }}
  >
    {(args) => (
      <FormGroup label="i am a large label" labelSize="large" error="error!">
        <Select error {...args} />
      </FormGroup>
    )}
  </Story>
  <Story
    name="FormGroupSmall"
    args={{
      options: ['Small', 'Quite little'],
      value: 'Small',
      sizeVariant: 'small',
    }}
  >
    {(args) => (
      <FormGroup label="i am small and have a label" showRequired>
        <Select {...args} />
      </FormGroup>
    )}
  </Story>
</Canvas>
