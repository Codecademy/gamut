import {
  border,
  typography,
  positioning,
  spacing,
  layout,
  shadow,
  flex,
  grid,
  background,
} from './props';
import { createSystem } from './core/system';

export const system = createSystem({
  border,
  typography,
  positioning,
  spacing,
  layout,
  shadow,
  flex,
  grid,
  background,
});
