import React from 'react';
import { FancyFormProps } from './types';

export const GridForm: React.FC<FancyFormProps> = ({ fields, submit }) => {
  return <h1>Grid Form Test</h1>;
};

export default GridForm;
