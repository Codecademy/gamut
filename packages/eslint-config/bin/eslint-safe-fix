#!/bin/bash

set -eu

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"

echo "Running eslint --fix on ${1}"

$(npm bin)/eslint --config $DIR/../autofix.js --ignore-path .eslintignore --fix "$1" || exit 0
