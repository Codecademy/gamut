import styled from '@emotion/styled';

import { LayoutMenuSection } from './LayoutMenuSection';

export const TopLinkSection = styled(LayoutMenuSection)`
  padding-bottom: ${({ theme }) => theme.spacing[32]};
`;
