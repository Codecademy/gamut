import { styled } from 'styled-system/jsx';
import { button } from 'styled-system/recipes';

// The Panda-built Gamut Button — shared by host AND remote across the MF boundary.
export const Button = styled('button', button);
