# Patterns

Patterns (e.g., layout patterns, card patterns) must come from `@codecademy/gamut-patterns`. Use the design's metadata to match the Figma component name to the correct pattern component. If the correct pattern is unavailable, leave a placeholder annotation rather than substituting a non-system asset.
